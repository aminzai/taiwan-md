# 2026-06-19-103748-manual（iteration #2）— 羅大佑 EVOLVE + /loop 多核心 git 撞牆

> 同一 session 第二輪（接 `2026-06-19-103748-manual.md` 第一輪台灣流行音樂）。**本檔寫於 multicore 分歧期，尚未 commit（避免加劇 main tree 分歧）— 待 tree 重整後補登 MEMORY.md 索引。**

## 做了什麼

**羅大佑 EVOLVE ship origin `3419f7d4b`**（3/19 attribution audit batch 第 4 篇）。短 bio 1789→~5100 CJK depth，非編年體「漂泊者唱失根」策展（A 放射科診斷視角開篇 → B 台北/紐約/香港/北京 29 年搬 19 次 → C 70 歲倖存者）。

- multi-agent v6.3：2 persona + viewpoint Opus(24 探索) + 2 research Sonnet(57 搜尋，81 總) + fresh Opus writer(staging) + media iframe agent。
- Stage 0 gate + Stage 1 depth gate + rewrite-stage-3-5 + rewrite-stage-4 全 hard=0；prose-health 2；3 CC 圖（2011 hero / 2021 金曲 / 縱貫線）+ 1 滾石官方〈鹿港小鎮〉iframe。
- **attribution audit 收穫**：鄉愁四韻 詞=余光中 / 滄海一聲笑=黃霑(羅只唱非作者) / 皇后大道東·似是故人來 詞=林夕 / 明天會更好 曲羅+7 人詞 / 滾滾紅塵 三毛=編劇 / 童年·光陰的故事 1981 張艾嘉首發(非 1982) / 你的樣子 1988(非 1984) / 棄醫「練唱」非「練吉他」。
- 政治 omit：2025 林夕罷免之爭（§自主權邊界 + 舊 bio whats_excluded）。
- research SSOT：`reports/research/2026-06/羅大佑.md`。

## 教訓（升 LESSONS 候選）— 最重要

1. **REFLEXES #31 救命**：media agent 回報 5 個「官方」YouTube ID，oEmbed 主 session 自驗發現 **3 個是「Lo Ta-yu - Topic」YouTube 自動生成假頻道**（pipeline Step 4.3.6 明禁）+ 1 個 UGC，只有 2 個真滾石官方。agent 的「官方」claim 是線索不是 oracle，每個 ID 必 oEmbed 驗 author_name/author_url。

2. **多核心 git 撞牆（本 session 最痛）**：`/loop` cron(acb184b7) + daily cron + 平行 manual session 全在**同一 shared working tree + 同一 local main** 上 commit/push → 嚴重分歧（local 5 ahead / 12 behind，端午節/立蛋 commit 在 local 與 origin 各有不同 hash 的重複版本）+ dirty 平行 WIP（視覺化型錄-recat session 的 memory/diary 未 commit 改動）。`git rebase --autostash` 撞 DIARY.md/MEMORY.md conflict + push 把平行 commit 推上 origin 但**我的羅大佑卡在 conflicted rebase 沒上去**。
   - **解法 = worktree 隔離 land**（鐵律 5 預設）：`git worktree add --detach <path> origin/main` → `git checkout <my-commit> -- <我的檔案>` 只取我的文章/圖/research（避開 INBOX/DONE-LOG 衝突）→ commit + push → remove。乾淨落地不擾動平行 WIP。
   - **worktree husky 坑**：detached worktree 無 node_modules → prettier ENOENT pre-commit fail。`ln -sfn ../taiwan-md/node_modules <worktree>/node_modules` 修（非 --no-verify，content 本就過 gate）。

3. **/loop 1h 節奏 < ~1.5h 深度文 = 永久 backlog stacking**：每篇嚴格 pipeline（多 agent + 81 搜尋 + 媒體深掃 + 驗證）要 ~1-1.5h，但 cron 每 1h fire → 永遠追不上 → cron 在我 busy 時 queue，stack 成一串。哲宇 manual 中斷 + cron 連環 fire 撞在一起。**已停 cron acb184b7**，待哲宇決定 cadence（建議 2-3h 或 self-paced，或接受 stacking = 連續產出）。

## Handoff（三態）

- **Done**：流行音樂(iter#1 `c8dec04f6`) + 羅大佑(iter#2 `3419f7d4b`) 兩篇 depth EVOLVE 都在 origin/main。/loop cron 已停。
- **Pending（待 tree 重整後）**：
  - 羅大佑 INBOX audit-batch 標 ✅ + DONE-LOG entry + 本 iter#2 MEMORY.md 索引 row — 都 drafted（local commit `1bbd8c219` 內 / 本檔），但**沒 commit 上 origin**（避免加劇分歧）。tree 重整後補。
  - main working tree 在 `1bbd8c219`（diverged，含平行 WIP dirty）— 需哲宇/乾淨 session 重整（勿 reset --hard，會掉平行 WIP）。
  - 羅大佑翻譯走巴別塔 / Stage 5 reverse cross-link（台灣流行音樂↔羅大佑）/ Stage 3.6 fan-out 被 /loop 重 fire 中斷（attribution 已 Stage 1 adversarial 深驗 + 主 session 自查關鍵 atom）。
- **Watch**：multicore shared-tree 是這個 /loop 模式的結構性風險 — 多 actor 同 tree 必撞。未來 /loop 寫文建議每 iteration 開 worktree（隔離），或限制同時只一個 writer actor。

🧬
