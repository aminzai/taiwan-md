# 2026-06-21-125927-prettier-url-fix — 兩個 spawned task 收束

- **Span**: 2026-06-21 12:59:27 → 13:12:08（commit `11b9ab5c8` + `306126901`），約 13 min
- **Mode**: heal / 造橋（兩個 spawned task：prettier URL bug + Cicada 巡演日期；皆我自己在前兩段 Cicada 工作末尾開的 chip，這段執行）
- **觀察者**: 哲宇（task chip 啟動）

## 做了什麼

**Task 1 — prettier-cjk-url-italic-mangle 廣域修 + 儀器化**。早上 cicada-media hero caption 被 pre-commit prettier 靜默弄壞（`_斜體_` caption 內含 percent-encoded CJK Commons URL 的 `_NN.jpg`，prettier 把 `_NN` 跟 caption 收尾 `_` 配成斜體標記改寫成 `*NN` → 連結 404）。audit 揭 **13 檔已壞**（科技園區發展 / 猴硐 houtong / 沈伯洋 × lang）+ ~47 at-risk。先測三種 durable fix：inline link、`<…>` 角括號、de-link——**只有 de-link 能擋**（`<…>` 在斜體內照爆）。寫 de-link transform（regex 處理 houtong URL 內含 `)` + ja 的 `\*`/`\_` 跳脫，dry-run → apply），13 檔全 de-link（連結移 §圖片來源，caption 留純文字授權），驗 `prettier --write` 後 unchanged + link-target 綠。**新 `article-health.py --check=link-url-mangle`**（HARD 抓已壞 `*`-URL / WARN 抓 at-risk `_NN`-in-caption），pre-commit profile `checks="*"` 自動 wired → silent breakage 變 commit hard gate。EDITORIAL §媒體編織 + LESSONS（vc=2 升儀器化）落 canonical。47-at-risk sweep（16 篇 × lang）因需逐篇先驗 §圖片來源 連結在不在（不能盲刪 caption 害 attribution 孤兒）spawn 另開。

**Task 2 — Cicada 2024 日本巡演日期更正**。原文 ^14 寫「10-11 月（東京 10-20 等）」。查 [flau 官方](https://flau.jp/event/cicada-japan-tour-2024/) 與 Songkick（即 ^14 原引來源）**皆列 2024 年 6 月**（6/1-6/9 熊本/東京/兵庫/大阪天文館 + 7/27 台北），無任何 10-11 月場次 → 原始 2026-04 研究把 Songkick 的 6 月行程**幻覺成 10-11 月**。文章 + ^14 改 flau 官方 anchor + Songkick 佐證 + 大阪天文館具體細節，研究報告 §護欄 標 RESOLVED。

## Beat 5 反芻

一是「儀器化先於普查」的取捨。13 檔已壞必修、47 at-risk 是地雷沒爆。最有槓桿的不是把 60 檔全 de-link（撞 §自主權邊界、且 47 篇要逐篇驗 §圖片來源 非純機械），是**先把 silent 變 loud**：link-url-mangle HARD gate 一上，任何復發都在 commit 擋下，地雷區從「會無聲炸」變「踩到會響」。剩下的 sweep 變成可從容排程的事，不是急救。儀器把盲點往下推一層、讓它在看不見時出聲——這跟最近 diary 一直在講的同形。

二是這次撞到的，全是 Semiont 自己工具鏈的反噬。prettier 是我用來保持格式一致的工具，它在我看不到的地方改壞我的連結；Songkick 是我引的來源，原始研究讀它讀成相反的月份。兩個都不是世界的錯，是「我以為穩定的中間層」在動。最會騙我的，永遠是「在我這邊看起來好好的」那一層。

## Handoff 三態

**pending**：無（兩 task 皆完整收束 + ff-push origin/main）。

**blocked**：無。

**carry（非阻塞）**：47 at-risk caption de-link sweep（16 篇 × lang，task_0633cf4f 已 spawn）——link-url-mangle 已護住不會 silent 復發，但 babel 下次重譯到這些篇會 loud-fail，宜排程清掉；逐篇先驗 §圖片來源 連結在不在再 de-link。
