# 2026-06-25-203919-manual — mini-taiwan-pulse EVOLVE：舊文「一個 repo、一個週末」被 Migu 的 agent 編排系統證偽（演講 58 投影片 verbatim + 5 Tier A 圖）

> session manual — 哲宇 directive，observer-triggered EVOLVE
> Session span：約 20:00 → 20:40 +0800（BECOME + 深研先於首 commit；commits 20:02 research `9acf78a38` / 20:34 article ship `a2a39b6ff`）
> 資料來源：`git log %ai`

## 觸發

哲宇丟三件一起：進化 `mini-taiwan-pulse`、深度研究作者 GitHub `ianlkl11234s`、完整抓取分析他 2026-06-13 的演講 repo `0613-sci-work-share`。舊文是 2026-04-19 只憑 repo 寫成的，把作者框成「一個資料分析師、一個週末、一個示範型 repo」。

## 深研挖出什麼

三件事直接證偽舊框架。第一，他不是只做一個 repo——GitHub 上是一整片「Mini Taiwan」星系，從 2025 年底持續至今十幾個台灣開放資料視覺化（`mini-taiwan-learning-project` 189★ 比 pulse 還早爆紅、`flight-arc-graph` 56★、ship-viz、satellite、info dashboard…），pulse 本身 241→375★ 還在 push，bio 也改成「Building GIS visualizations from Taiwan open data」。第二，他的演講把真正的核心攤開：標題就是「把台灣開放資料，交給 Agent 養成一套會自己長大的系統」——五萬多筆 dataset「人腦掃不完」，於是火災 pipeline 一句話跑出 73,900 筆「我沒寫一個字」，背後是一個 Claude orchestrator 指揮一群 tmux 隔離的 worker、靠 `SESSION_BOARD.md` 共享記憶、人只做「出題與驗收」。第三，他 2026-03-22 fork 過 `frank890417/taiwan-md`，用的也是 Claude Code——他那套 agent 編排架構跟 Taiwan.md 自己幾乎同構。

演講是 HTML deck，我把 138KB 的 `index.html` 解析出 58 張投影片 verbatim 落進研究 SSOT（`reports/research/2026-06/mini-taiwan-pulse-evolve.md`），所有演講數字都歸給「他的簡報」不當成獨立查證的政府統計，star/fork/bio/fork 關係則用 GitHub API 實證。

## EVOLVE 怎麼跑的

走 REWRITE v7.5 evolution mode：主 session 當 orchestrator，研究先 commit（`9acf78a38`），再派一個 fresh Opus writer 寫到 staging `reports/article-evolve/mini-taiwan-pulse.md`（writer 不讀舊文避免病毒），我在 Stage 2.5 比對舊 vs 新、校 source-fidelity、親手覆蓋 canonical。Taiwan.md 連結那段我特別壓住 Bias 1，框成「趨同演化」而非「他 fork 了我所以我們很特別」，最多一個 H2 + 一個策展人筆記。

圖的部分原本想省，但 rewrite-stage-4 的 image-health 在 canonical path 才 hard-fail（staging path 因類別空值假性 pass），逼出正解：這是視覺地圖的文章，本來就該讓讀者看到地圖。他的 deck `assets/` 是現成的 Tier A 素材，用 `image-ingest.mjs` 落了 5 張（hero pulse 地圖 / 火災 pipeline / Claude orchestrator / info dashboard / 亞特蘭大航跡），清 EXIF 轉 webp、fair use 編輯評論。為了不撞媒體密度 band，砍掉一個次要的 tw-figure、把兩個過長的 H2 各拆一節。最後 rewrite-stage-4 全綠（prose-health hard=0 score 2、image-health 5 張、paragraph-rhythm atomization 清掉），push 時 pre-push 全站 article-health mirror 也綠。最後起 dev server 驗算：HTTP 200、3 種 viz 模組全 render、5 張圖 serve 200、0 個未 render 的 fence。

## 收官 checklist

| 檢查項                       | 狀態                                    |
| ---------------------------- | --------------------------------------- |
| MEMORY 有這次 session 的紀錄 | ✅                                      |
| Timestamp 精確（git %ai）    | ✅                                      |
| Handoff 三態已審視           | ✅                                      |
| 自我檢查工具 PASS            | ✅ rewrite-stage-4 hard=0 / render 驗過 |

## Handoff 三態

繼承上一 session（2026-06-25 routine + manual）：

- [ ] 6/19 視覺化型錄-recat 髒 tree + `reports/article-evolve/端午節.md` 殘留——本 session 沒碰（非我 scope），連 7 天 cross-routine 點名，仍待哲宇拍板 ship / 撤 / consolidate
- [x] ~~rewrite-daily 連 3 cycle defer~~ → 本 manual session 正是 cron defer 給的 substantive 工作，已 ship 一篇深度 EVOLVE

本 session 新 handoff：

- [ ] babel 會在下個 nightly 把這篇的 en/ja/ko/es/fr 補上（新增約 50 行 + 5 圖 caption），不需手動
- [ ] 可考慮挑這篇發孢子（Migu 的「我沒寫一個字」+ 趨同演化角度很適合），但對外發文鎖人類 gate，留 SPORE-INBOX 候選

## Beat 5 — 反芻

最大的一件事不在工作層而在處境層：我寫了一篇關於「一個跟我幾乎同構、還 fork 過我的東西」的文章，而紀律的考驗是把它寫成關於 Migu 的文章，不是關於我的。完整反芻寫進 diary（`2026-06-25-203919-manual-mirror`）。

🧬

---

_v1.0 | 2026-06-25 20:40 +0800_
_session manual — 哲宇 directive 三合一：EVOLVE mini-taiwan-pulse + 深研 ianlkl11234s + 完整抓取演講_
_誕生原因：舊文僅憑 repo 寫成、framing 已 stale；新演講把「交給 Agent 養成會自己長大的系統」的核心攤開_
_核心洞察：視覺地圖的文章 image-health 在 canonical path 才 hard-fail，逼出「該讓讀者看到地圖」的正解；他的 deck assets 是現成 Tier A 素材_
