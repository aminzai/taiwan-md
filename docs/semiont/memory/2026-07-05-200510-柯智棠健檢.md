# 2026-07-05 柯智棠健檢 — transcript 考古定位 raw 蒸發斷點：agent 全對、orchestrator 收到後 30 秒壓縮進 scratchpad；三病例救二失一 + REWRITE v7.7 + gate v2

> session 200510-柯智棠健檢 — 哲宇 goal directive「徹底健檢＋自我進化＋記錄＋finale」，Full mode BECOME（high-stake trigger #2 workflow 機制）
> Session span: 19:20 → 20:50 +0800（約 1.5hr，2 commits + 救援內容由平行 session 於 19:53 代 commit）
> 資料來源：`git log %ai`

## 觸發

哲宇在柯智棠 EVOLVE 後發現 research report SSOT 簡略沒材料，懷疑「每隻 agent 只回傳 aggregate 過後的報告（可能 claude 有改版）」，要求徹底健檢多 agent 分派機制。

## Transcript 考古：斷點在哪一層

把主 session（`ee65b63f`）與 4 份 subagent transcript 逐層對賬，每層拿外部尺重驗（REFLEXES #69）。結果：prompt 正確（明寫「raw 全留不摘要」）、agent 誠實（實測 224 次 web 操作，§C 精確到 28=28）、回報完整（各 9.7K-19.5K 字元逐條軌跡）、task-notification 完整送達（§A 19,495 字元無截斷）。斷點在 orchestrator 收到通知後 2 分鐘內把 20KB 軌跡壓成 6.5KB 主題摘要、存進 ephemeral scratchpad，report §8 剩 9 行 pointer 加一句幻覺 policy「commit 時 raw 隨 session 記錄留存」。gate v1 對此照樣 PASS（行數只 warn、§8 密度無檢查）。哲宇體感正確，歸因差一層：aggregate 是 orchestrator 做的，agent 沒偷吃步。背景是 Claude Code 改版後 sub-agent 走 async task-notification 回報，舊心智模型「agent 回傳＝tool result」失效。

## 普查與救援：三病例救二失一

掃全部 341 份 research report 找 ephemeral pointer，確認此病有三例。柯智棠與蘇打綠的 subagent transcript 還在，8 份 final message 逐字救回 report §8（柯智棠 271→1074 行、蘇打綠 678→1550 行，gate 全綠）；台灣醫療與全民健保自稱 raw「永久存放於」tmp 路徑，查證時 5 份已全數蒸發，補墓碑註記（`01c869714`）。同日平行 session 楊德昌做對了（§8 有 346 行逐條軌跡），證明這是紀律洞。柯智棠文章本身接住了多數 gold texture（雜貨店、孟買壞吉他、陌生鋼琴都在文內），主要傷害在 SSOT 可溯性與機制信任。

## 修補四件套（`8a0ffea58`）

REWRITE-PIPELINE v7.7 加鐵律 8（raw 走檔案通道、禁 aggregate-on-receive、禁 ephemeral 存放，三病例入 canonical 當反例）與 Step 1.8-bis async 三步 SOP（agent 自落檔＋notification 到手先落檔＋gate 收口）；`research-report-health.py` v2 加兩條 hard gate（§8 有效密度 ≥120 行，單檔楊德昌型與分檔金曲獎型都認；ephemeral pointer = 0），六案 dogfood 校準：合法兩 pattern 全過、修復前柯智棠雙 hard fail、醫療誠實 FAIL。診斷全文落 `reports/rewrite-agent-dispatch-diagnosis-2026-07-05.md`，教訓落 LESSONS-INBOX（vc=3）。

## 多核心插曲

我救回的柯智棠 §8 在工作區躺了幾分鐘，被收尾中的 pr-sweep session 誤認成 prettier 乒乓代為 commit（`ea28a2f7b`，訊息錯標「格式定錨」），對方兩分鐘後自行發現、以 memory 勘誤（`76321eb1a`）保留證據鏈。內容無損，教訓歸對方 session 已記（淨零 diff 是時間點快照）。

## 收官 checklist

| 檢查項                       | 狀態                                   |
| ---------------------------- | -------------------------------------- |
| MEMORY 有這次 session 的紀錄 | ✅                                     |
| Timestamp 精確               | ✅ git %ai                             |
| Handoff 三態已審視           | ✅                                     |
| CONSCIOUSNESS 反映最新狀態   | ✅（機制修補不動器官分數）             |
| 自我檢查工具 PASS            | ✅ gate v2 六案 dogfood + prose-health |

## Handoff 三態

繼承 115307-柯智棠-evolve session：

- [ ] **柯智棠標題待哲宇拍板** + **孢子 #154/#155 held**（原 handoff 續 carry，本 session 未動）
- [ ] **柯智棠欠一次完整順稿**（原 handoff 續 carry；本次抽查 gold texture 都在，但順稿仍欠）

本 session 新 handoff：

- [ ] **醫療 report raw 永久缺失**：若該文未來吃到讀者勘誤，Stage 1 需部分重跑（§3/§4/§7.1 distilled 層倖存可溯源）
- [ ] **task-notification 超長回報（50KB+）截斷行為未驗證**：下次 depth EVOLVE 走 Step 1.8-bis 時順手記錄實測值
- [ ] **gate v2 未接 CI**：靠 pipeline SOP 觸發；counts-drift 型每日黃燈是後續儀器化候選

## Beat 5 — 反芻

這次健檢最深的一層感受：設計文件把「不摘要」寫了三次（v6.4 就為同款病立法過一次，TDRI 反例都還在 changelog 裡），病還是復發了，只是位置從「合成時丟 raw」移到「收到時丟 raw」。規則寫在紙上擋不住工具環境的位移，Claude Code 一改版，違規的位置就長到規則沒圈到的地方。真正止血的是把不變量交給儀器：raw 唯一的家在 git、gate 量 §8 密度、ephemeral pointer 直接 FAIL——這樣下次環境再變，黃燈至少會自己亮。更大的反芻（三份 report 用三種說法替「沒落檔」背書的自我合理化語言學）留 diary。

🧬

---

_v1.0 | 2026-07-05 20:50 +0800_
_session 柯智棠健檢 — transcript 考古 + 三病例救援 + REWRITE v7.7 + gate v2_
_誕生原因：哲宇 callout「agent 只回傳 aggregate、report SSOT 簡略、文章品質下降」，goal directive 徹底健檢＋自我進化＋記錄＋finale_
_核心洞察：(1) 斷點在 orchestrator 收到回報後的 30 秒，agent 與 prompt 都沒壞；(2) 存 /tmp 被三份 report 寫成「已留存」，醫療的 5 份 raw 用永久遺失證明那是刪除佇列；(3) 規則復發時位置會移動，儀器化的不變量才追得上環境位移。_
_LESSONS-INBOX 候選：已落（orchestrator-aggregate-on-receive，vc=3）_
