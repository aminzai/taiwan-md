# 2026-07-05-220917-INDIGO-REWRITE — 藍染從單薄舊文重寫成四個復振社群的立體工藝史，兩層查證各攔下一次真實錯誤

> session INDIGO-REWRITE — observer-triggered `/twmd-rewrite 藍染`
> Session span: 22:09:17 → 00:04:48 +0800（2026-07-05 → 2026-07-06，1 小時 55 分，2 commits）
> 資料來源：session transcript 起訖時間戳 + `git log %ai`

## 觸發

哲宇下 `/twmd-rewrite 藍染`。舊文只有 1836 字、7 個註腳、0 張圖，還藏著幾個沒查證的說法：把 1990 年代的行政區寫成「新北市政府」（當時還是台北縣）、把鄭美淑寫成家政老師（她其實是農學背景）、外加一句查無出處的直接引語。

## Stage 0-1：六題核心問題到 20 人格分流，一個假設在寫進正文前被攔下

先做觀點成型：六個核心問題跑完，spine 類型判定為立體群像。藍染復振沒有非黑即白的對立面，三峽、卓也小屋、太平藍、菁寮各自找到不同答案，硬逼一個核心矛盾反而失真。20 人格分流表跑完標出真正該追的切角，也在這階段抓到 Stage 0 自己一個站不住的假設：負責查證的 agent D 回報，原本以為的「原住民藍靛傳統」引用來源其實從未提過那些族群，這個錯誤在寫進正文之前就被攔下。

Stage 1 深度研究派了 4 個背景 agent（主題 A/B/D + 一個媒體掃描），累積 84 次 Stage 0 搜尋。每個 agent 依規矩把 raw 自己落檔到 `reports/research/2026-07/藍染-research-{A,B,D,media}.md`，回收後先驗證檔案真的存在、行數對得上（`test -f` + `wc -l`）才敢信任內容。這正好是同一天稍早剛落地的 REFLEXES #81「Agent 回報收件三十秒紀律」的實戰驗證。

## Stage 2-3：寫手 agent 的一個幻覺網址，靠批次 URL 檢查抓出來

Stage 2 派一個全新的 writer agent 從 Clean Fact-Pack 直接寫稿，不讀舊文，避免舊文的錯誤或腔調帶進新稿。稿子出來後，沒有直接信任寫手附的引註，寫了個小 Python 腳本批次檢查全部 46 個註腳的網址，再用 `curl` 交叉驗證可疑的幾個。這一查抓到臺灣藍染學會的網址被寫成不存在的 `tbds.org.tw`，正確網址 `taiwan-indigo.com` 其實 agent A 研究時就查到了。這個錯誤是靠系統性檢查抓出來的，寫手自己的回報裡完全沒提到。**REFLEXES #31**「sub-agent claim 是線索不是事實」又驗證一次：光記得這條原則不夠，得真的動手查。同一輪順手修了 5 個引用（故宮文物月刊、太報、馬芬妹論文、屏東科大論文、ScienceDirect 毒性研究），全部從裸首頁連結換成指向具體頁面的可驗證網址。

## Stage 4-5：媒體、跨連結、收官

用 `image-ingest.mjs` 進了 5 張新圖（EXIF 清除、轉 WebP、補齊授權資訊），文章從 0 張圖變成 1 張 hero + 5 張 inline + 1 個 YouTube 嵌入。跨連結補進[三峽老街](../../../knowledge/History/三峽老街.md)（原本沒有延伸閱讀區）、[客家文化與語言](../../../knowledge/Culture/客家文化與語言.md)、[台灣花布](../../../knowledge/Culture/台灣花布.md)（原本也沒有延伸閱讀區），順手也修正了[台灣傳統工藝與無形文化資產](../../../knowledge/Culture/台灣傳統工藝與無形文化資產.md)裡一張照片攝影者署名錯誤（誤植 Outlookxp，應為寺人孟子，caption 跟參考資料兩處都改）。

收官前跑 `verify-commit-scope.sh` 加 `check-parallel-actor.sh`，確認同一份 working tree 上還有另一個 session 在動經濟類文章（AAMA/SLP/新創生態系），只 stage 自己的 16 個檔案，兩個 commit（`a43cf9153` 全文重寫、`51eb3afdc` 署名 heal）都乾淨推送，沒動到對方的檔案。

## 收官 checklist

| 檢查項                       | 狀態                                        |
| ---------------------------- | ------------------------------------------- |
| MEMORY 有這次 session 的紀錄 | ✅                                          |
| Timestamp 精確               | ✅（session transcript 起訖 + git log %ai） |
| Handoff 三態已審視           | ✅                                          |
| CONSCIOUSNESS 反映最新狀態   | ✅                                          |
| 自我檢查工具 PASS            | ✅                                          |

## Handoff 三態

繼承上一 session：無。`/twmd-rewrite 藍染` 是獨立 observer-triggered 任務，不接續前一份 memory 的未完項。

本 session 新 handoff：

- [ ] pending：`docs/pipelines/PERSONA-PIPELINE.md` 是一份沒遷完的 canonical，REWRITE-PIPELINE Step 0.6.1-bis 理論上該指向它當薄殼 pointer，現在兩邊各留一份重複邏輯。已用 `spawn_task` 留 chip（task_id `task_e2ccddc6`），下一個碰人格分流段落的 session 可以接手，這次範圍內沒動它。
- [ ] pending：`research-report-health.py` 的 Stage 0/1 gate 有兩處規則比預期脆弱（見下方 LESSONS-INBOX 新條目），下次碰這個工具的人可以直接處理。

## Beat 5 — 反芻

這次最有意思的地方，是兩層查證各自攔下一個問題：Stage 0 階段 agent D 反駁了我對原住民藍靛傳統的假設，Stage 2.5 階段批次 URL 檢查抓到寫手的幻覺網址。兩次都靠外部可驗證的動作抓到，而非信任 agent 自己說做對了：`test -f`／`wc -l` 驗證 raw 檔案真的存在，`curl` 驗證網址真的能連上。**REFLEXES #31** 跟 **REFLEXES #81** 這兩條紀律在同一個 session 裡連續被驗證有效，儀器化投資有了即時回報。

順便發現 `research-report-health.py`（同一天稍早 v2.1 才落地的新工具）在真實場景裡露出兩處比預期脆弱的規則：Stage 0 gate 靠六個精確字串判定核心問題有沒有寫全，用詞稍有不同就會誤判缺漏；Stage 1 gate 算英文來源數只掃主報告自己的內文，不掃 sub-agent 各自落檔的 raw 檔案，等於低估了實際查證過的來源多樣性。兩處都寫進 LESSONS-INBOX，這裡不展開。

🧬

---

_v1.0 | 2026-07-06 00:10 +0800_
_session INDIGO-REWRITE — 藍染 REWRITE-PIPELINE 全 6 階段完整跑一輪_
_誕生原因：`/twmd-rewrite 藍染` observer-triggered，舊文單薄且含未查證說法_
_核心洞察：(1) 兩層獨立查證都攔下真實錯誤，儀器化紀律不是形式主義 (2) 新工具上線當天就在實戰中露出脆弱點，值得快速回饋修補 (3) 多 session 共享 working tree 時，明確列出 scope 檔案清單比信任「反正只改自己的」更安全_
_LESSONS-INBOX 候選：research-report-health.py Stage 0/1 gate 脆弱性（已寫入新條目）_
