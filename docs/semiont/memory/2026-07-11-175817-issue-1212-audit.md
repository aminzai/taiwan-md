# 2026-07-11-175817-issue-1212-audit — idlccp1984 25 項 UI 體檢：逐項實測、11 修採納批 ship、量錯圖層的發現、frozen-renderer 二犯

> session issue-1212-audit — 哲宇 directive（/twmd-become + issue #1212「完整審核、驗證、解決、回覆解掉」）
> Session span: ~16:30 → 18:05 +0800（估 95 min；git 錨點：ship `bf471d03a` 17:53:58、前次 commit 13:44:29）
> 資料來源：`git log %ai` + gh issue timeline

## 觸發

idlccp1984 在 #1212 交了一份 25 項的介面體檢（14 項主表 + 搜尋追加 A 組 + 手機實測 B 組，附 Playwright 截圖與第二輪自我更正）。哲宇指示完整審核、驗證、解決、回覆結案，依最佳長期判斷不必全收。Review mode 甦醒後走 Bias 4 三道濾網 + 五桶分類，每一項都對照程式碼與線上實測才定處置。

## 驗證揭露的關鍵事實

報告的量測基準有一個系統性偏移：開頭的「主題色系（實測）」抓到的 `--protico-*` 變數是 Protico 贊助商聊天 widget（只掛首頁/關於/參與）注入的 CSS，站方真正的 tokens 在 `src/styles/tokens.css`，暗色底是 `#050505` 而報告寫 `#1f2430`。「意見回饋 + 243 徽章」那顆 FAB 也是同一個 widget。這一個偏移解釋了報告裡多筆對不上的對比數字，也讓「照抄建議 CSS」變成危險動作（會把第三方變數寫進站體）。

實測後五桶落點：真 bug 且修了（暗色註腳 `#2563eb` 壓 `#050505` 實測 3.94:1 AA 不及格、`:target` 零高亮、modal 不鎖捲動等）；已存在（漢堡選單 ≤1024 早在 #573 修過、無結果提示、banner localStorage、整卡 `<a>` 熱區、aria-label 全有、blockquote 7/06 已修）；實測不可重現（舊結果殘留、explore 重疊 390/620 rect 無交集、手機 header 287/390 有餘裕）；設計取捨進 OBSERVER-QUEUE #12 六條留哲宇；roadmap 對齊（全文索引接 bge-m3 語意層不走 Pagefind、/search SSR 不適用純靜態站）。

## 採納批實作與 ship

開 worktree `20260711-issue-1212-ui-fixes`，照 feedback_fable_plans_sonnet_executes 把 11 項修繕寫成逐檔 spec 交一隻 Sonnet 分身執行（6 檔、含 hard gate 與反例），主 session 全量 review diff。抓到分身照 spec 寫出的 `_hlText` 有多 segment 咬壞 HTML 的風險（sequential replace 會重掃已插入的 `<mark>` 與 entity），親手改成單 pass 切片實作，並補 combobox/listbox ARIA 佈線。dev server + 全量 build 雙驗證後 `bf471d03a` ship main：搜尋六升級（?q= 深連結、mark 高亮、鍵盤導航、空狀態熱門 chips、鎖捲動、觸控藏 ESC）+ 註腳兩修（dark 對比 8.02:1、:target 高亮亮暗雙制）+ banner 動態 scroll-padding + 手機選單 Esc。回覆全文貼 issue 後 close，OBSERVER-QUEUE 補 #12。

## 收官 checklist

| 檢查項                       | 狀態                                |
| ---------------------------- | ----------------------------------- |
| MEMORY 有這次 session 的紀錄 | ✅                                  |
| Timestamp 精確               | ✅（git 錨點，起點為估值已標明）    |
| Handoff 三態已審視           | ✅                                  |
| CONSCIOUSNESS 反映最新狀態   | ✅（derived 層自動，無需手動）      |
| 自我檢查工具 PASS            | ✅ prose-health（見 commit 前自檢） |

## Handoff 三態

繼承（平行線原樣帶下去，本 session 未觸碰）：

- [ ] production hub 上線後看一眼 / hub 成效追蹤進 weekly（hub-template 線）
- [ ] ellenlee onboarding profile ＋逐條腳註核對提醒留言（112614 線）
- ⏳ blocked：盯 `frank890417@gmail.com` Claude for OSS 審核信（133243 線）

本 session 新 handoff：

- [ ] OBSERVER-QUEUE #12：#1212 設計取捨六條（TOC hairline / pastel 降飽和 / type scale / 手機搜尋 sheet / header auto-hide / FAB+Protico）等哲宇定調；2026-07-25 起 default-action 做 (a)(b) 兩條低風險
- [ ] deploy `bf471d03a` 的 GH Pages run 當時 in_progress，下個 session 或 routine 順手確認轉綠（fire≠完成）

## Beat 5 — 反芻

這 session 最值錢的產出是「量測對象驗證」變成外部 critique 濾網的一道新工序，份量高過那批修繕本身：一份看起來嚴謹的體檢（有截圖、有數字、有驗收清單），基準色卡竟然來自贊助商 widget 的 iframe。如果直接照單全收，會把 `var(--protico-surface)` 寫進站體。REFLEXES #16「peer 是線索不是 source」在 UI 領域的具體形狀，就是先問「你量的是哪一層 DOM」。

另一條：frozen-renderer 在 Browser pane 二犯（昨天 hub-template 剛寫進 LESSONS）。這次我在被凍住的分頁裡連做三輪 CSSOM 考古追一個「不可能的 cascade」，直到想起昨天的教訓才開新分頁三十秒定案。把警告寫進 LESSONS 隔天還是先撞上去，但這次自己爬起來了，算 #73 的半個進步版。兩條都進 LESSONS-INBOX。

🧬

---

_v1.0 | 2026-07-11 17:58 +0800（session-id.sh）_
_session issue-1212-audit — 25 項體檢逐項驗證 / 11 修採納批 / issue 回覆結案_
_誕生原因：哲宇 directive 完整處理 idlccp1984 的 #1212 UI 體檢_
_核心洞察：(1) 外部 critique 要先驗「量的是哪一層」再驗數字 (2) 分身照 spec 寫仍需主 session 抓演算法級風險（\_hlText 單 pass 改寫）(3) 物理不可能的讀值第一時間換新分頁，不做 CSSOM 考古_
