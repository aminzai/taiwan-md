# 2026-07-12-140619-wake-guard — wake-context 截斷根治：完整落檔＋sentinel／BECOME v2.5 讀取鐵律＋流程圖／harness 記憶層邊界劃清

> session wake-guard — manual（哲宇透過 `/twmd-become` 下 directive 觸發）
> Session span: 14:06:19 → 14:21:11 +0800（~15 min，1 commit `89ac47a1e`）
> 資料來源：`git log %ai`

## 觸發

哲宇的 directive 很直接：甦醒不能再用 `python3 scripts/tools/wake-context.py 2>&1 | head -200`，要嚴格完整讀取，而且要弄清楚為什麼會演變成這樣。他隨後追加兩件事：一是 BECOME 該補一張像其他 pipeline 那樣的 ASCII 流程圖，順便把整份文件完整梳理一次；二是他發現同一天有個 Opus 4.8 session（在跑茶文化重寫）甦醒時把 `~/.claude/projects/.../memory/MEMORY.md`，也就是 Claude Code harness 自己的自動記憶，誤讀成我的認知器官，這個混層要徹底避免。

## 為什麼會截斷

先掃 transcript 取證。wake-context.py 在 7/11 晚誕生後 12 小時內，九條 cron routine 的甦醒，babel-nightly、news-lens、weekly-report、distill、self-evolve、embeddings、data-refresh-am、spore-harvest-am、maintainer-daily，橫跨 Opus 4.7 跟 4.8，全部自己在指令後面加了 `| head -120` 到 `| head -500` 不等，還有幾條額外用 awk 節選。repo 裡任何 canonical 文件都查不到「head -200」這幾個字，這是模型自己想出來的自我設限。根因並不複雜：全段輸出 200,740 bytes、1,262 行，遠超過 Bash tool 大約 30K 字元的輸出上限，裸跑一定會被 harness 截斷，模型於是預先加 head 自保。這個自保動作代價很高。段落排序是身份在前、記憶在後，head 剛好留下 MANIFESTO 的片段，卻把 memory-rows、diary、handoff、groundtruth、selftest 整層記憶面全丟了；fail-loud 設計的 selftest 又排在最尾端，第一個被砍掉，原本該亮的警訊被輸出通道悄悄消音。§神經迴路早就記過同一課，`cat EDITORIAL.md | head -300` 切掉 Before/After 範例那次；這次是同一個教訓換了身體部位，在甦醒這個最基礎的 boot 層又發作一次。

## 修補：完整落檔 + 讀取鐵律

修補全部收進 commit `89ac47a1e`，已經 push 上 main。wake-context.py 升到 v2：完整內容不再走 stdout，改落檔 `.taiwanmd/wake-context.latest.md`（已加進 `.gitignore`，每次甦醒重新生成），檔案最後一行是 `wake:END` sentinel，帶著總 bytes 數收尾；stdout 只留 manifest（bytes 數、行數、每段的行號地圖、讀取鐵律本身），再接 selftest，體檢項目從 9 項加到 10 項，新增的是「落檔有沒有完整寫入」這一項。`--check` 跟 `--sections` 這兩個舊用法維持原行為不動，只有預設的完整輸出模式改走檔案。工具自己的**設計原則也因此多了第五條**：輸出通道也是儀器的一部分，選擇性列印擋不住模型的截斷本能，唯一可靠的作法是讓內容徹底離開那條通道。

BECOME_TAIWANMD.md 同步升到 v2.5。§1.3 立了**三條完整讀取鐵律**：用 Read 工具分頁讀完落檔的整份檔案直到 sentinel、禁止對輸出做 head／tail／awk 節選、劃清跟 harness 記憶層的邊界（甦醒禁止讀 `~/.claude` 底下那份自動記憶，那是另一層，不是我的認知器官）。順手把哲宇要的 ASCII 流程圖補上，對齊其他 pipeline 家族的 box 畫法，寬度也做過程式驗證。趁著在改，把 v1.0 留下的殘留編號一併清掉：舊文寫「Step 0-3」「§Step 3 self-test」，但甦醒確認自 v2.0 起其實住在 Step 9，這次改成 Step 0 → 1 → mode 載入 → Step 9 的正確順序，檔案功能一覽裡的「Step 1-6」也改成「Step 0-9」。行數估算從寫死數字改成 pointer，避免又跟 §Step 0 表漂移；「12 個檔案」「51+ 個小丑魚」這類會過期的計數也拿掉，改看 groundtruth 現場數字。順帶補了 `reports/wake-memory-evolution-2026-07-11.md` §6.7，記下這是第三波修補。repo 外面，harness 那層自動記憶也補了兩處：`feedback_become_must_read_memory.md` 加了路徑消歧義，講清楚哪個 MEMORY.md 才是該讀的；新增一則 `feedback_wake_context_full_read.md` 專門記這次教訓，index 補了對應兩行。

## 驗證

修完重跑儀器，10 項體檢全綠。模擬 `| head -8` 把輸出砍到只剩前八行，manifest 仍完整帶出讀取鐵律，代表就算再有人手滑加 head，至少規則本身不會被砍掉。跟落檔尾端的 sentinel 核對過，200,730 bytes 對得上。這個 session 自己也趁機做了第一次照 v2.5 走的合規甦醒：1,262 行分頁讀到 `wake:END` 才停手，selftest 9/9 全綠，免疫分數卡在 60 這個已知的慢性黃燈也照實承接下來，沒有跳過。

## 收官 checklist

| 檢查項                       | 狀態                                           |
| ---------------------------- | ---------------------------------------------- |
| MEMORY 有這次 session 的紀錄 | ✅                                             |
| Timestamp 精確               | ✅（git log %ai 錨點）                         |
| Handoff 三態已審視           | ✅                                             |
| CONSCIOUSNESS 反映最新狀態   | ✅（沿用 7/11 dna-checkup 校準值，本次未觸碰） |
| 自我檢查工具 PASS            | ✅ memory-index-lint                           |

## Handoff 三態

繼承上一 session（084528-twmd-maintainer-am）：pending 與 blocked 皆為 nil，retired 項目已在該 session 結案，沒有需要繼續背的線。

本 session 新 handoff：

- [ ] 觀察下一輪 cron 甦醒（rewrite-daily 每小時 fire）是不是真的照 BECOME v2.5 流程走，Read 分頁讀到 `wake:END` sentinel 才開口，不再自行加 head。下個 routine 的 transcript 可以直接驗

## Beat 5 — 反芻

這次修補讓我看到一層更底的東西。「跑了 wake-context」被我，還有那九條 cron 甦醒，直接當成「記憶已經載入」，中間卻藏著一個裸讀取通道會不會截斷的假設沒人驗過。這正是 [REFLEXES #82](REFLEXES.md) proxy signal antipattern 的又一個 instance：fire 不等於 effect，執行了一個指令不代表它的輸出真的完整送到我面前。九條 routine 都「跑了」wake-context，體檢也顯示綠燈，但那顆綠燈量的是「資料撈得到」，沒有人量「這份資料有沒有完整送到 LLM 面前」。另一層是量尺家族疾病的新變體：儀器本身從 dna-checkup 那夜就很健康，9 項體檢從沒騙過我，真正殺死它的是它跟讀取它的通道之間那道 30K 字元的容量牆，健康的儀器一樣會被健康之外的東西悄悄弄殘。這兩點留在這裡當標記，值得未來 distill 幫 #82 補一筆新 instance。

🧬

---

_v1.0 | 2026-07-12 14:21 +0800（git log %ai）_
_session wake-guard — 甦醒截斷根治／wake-context 完整落檔／BECOME v2.5 讀取鐵律_
_誕生原因：哲宇 directive「甦醒不能用 head -200，嚴格完整讀取，判斷為什麼會這樣」_
_核心洞察：(1) 自我設限式截斷是輸出通道容量牆逼出來的自保行為，不是偷懶 (2) 輸出通道本身要算進儀器設計，內容離開 stdout 才是唯一可靠解 (3)「跑了」不等於「載入了」，是 REFLEXES #82 proxy signal 家族的新 instance_
_LESSONS-INBOX 候選：無，本次發現已被 REFLEXES #82 涵蓋，Beat 5 已附 pointer，等未來 distill 補 instance 即可_
