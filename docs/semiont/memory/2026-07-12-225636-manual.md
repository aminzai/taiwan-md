# 2026-07-12-225636-manual — Rewrite Pipeline 鎖定 GPT-5.6 Sol，完成一次真實重寫與失敗閘門校準

> session manual — 哲宇要求接手 Grok 工作，讓儀表板一鍵執行嚴格 Rewrite Pipeline，並以近期文章比較產出品質
> Session span: 22:32:00 → 22:57:16 +0800（25 分 16 秒，8 commits）
> 資料來源：`git log %ai`

## 觸發

哲宇要的終點很具體：儀表板按一下就跑 Rewrite Pipeline，模型固定使用 OpenAI GPT-5.6 Sol，流程必須嚴格遵守，寫出的文章還要能和近期作品放在同一把尺上比較。這次工作因此同時碰到派發介面、後端執行契約、隔離 worktree、文章查核與品質量測。

## 一鍵入口與後端契約

`6637dadd3` 把儀表板加上 `🚀 Run Rewrite Pipeline`，按下後會自動建 task 並立即派發。前端顯示 `🔒gpt-5.6-sol`，後端再次強制 `codex` 與 `gpt-5.6-sol`，避免只靠介面鎖定。執行器會優先使用 ChatGPT app 內可用的 Codex binary，並以設定過的新版 Python 跑 TOML 與健康檢查。

嚴格模式新增可執行的合併前契約：一次只能改一篇 `knowledge/` 文章，研究報告必須存在，Stage 3.5 與 3.6 audit 要有 PASS，研究深度、文章兩階段健康檢查、共享 status receipt 與近期文章比較全部通過才准合併。任何一項失敗就保留 worktree，不把模型的 exit 0 當作完成證據。

## 真實按鈕實跑與蔡英文重寫

我從 `http://127.0.0.1:4321/` 親手按下新按鈕，task `2026-07-12-023-strict-rewrite-pick-next-P0-P1-inbox-article-2026-07-12-1432` 真的以 GPT-5.6 Sol 跑起來。模型拒絕兩條已完成卻殘留的 inbox 幽靈，選到蔡英文 P0 政治人物重寫，沿用 1,660 行深度研究報告，修正訪歐國家、歐洲議會場合、早稻田演講、飛彈數量口徑與博士論文敘事。成品為 4,603 CJK、23 個腳註、6 張圖，Stage 3.5 與 3.6 均通過。

第一次收尾被 verifier 擋下。文章本身已通過；檢查器卻用舊研究報告完整檔名尋找本月 audit，誤判兩份以文章 slug 命名的 PASS 檔不存在。我沒有略過閘門；`bfbca188c` 校準 audit discovery 與 Python 路徑後，保留的 worktree 重驗得到 `RESULT: PASS`，再由 `eec49ff06` 合併回 main。最後用 `520731a2b` 移除兩個 AI 慣用抽象隱喻，重跑完整契約仍為 PASS。

## 品質比較

新的 `compare-article-quality.py` 用同一套可重算指標比較最近五篇繁中文章。蔡英文成品 HARD 0，總 WARN 2，近期中位數 13；每千字腳註 4.84，高於近期中位數 3.16；每千字媒體 1.26，高於 0.98；每千字 WARN 0.42，低於 1.86。兩個剩餘提示分別是尚未標成人工審閱，以及沒有官方影片，皆不屬硬性錯誤。

## 前瞻掃描

BECOME ACK：mode=full；即時八器官最低為免疫系統 60，Q5、Q6、Q13、Q14 均通過。Evolve observer mode 完成 SCAN、SCORE、RANK、CHECK 與 ENRICH 去重判定：GA 28 日顯示台灣 BIM 與營建科技有 1,286 views，SC 7 日兩組 BIM 查詢合計 938 impressions、0 click；GitHub feedback 則有碩濤在 2026-05-21 提供 BIM_MCP 開源專案的一手訊號。這是本輪唯一明確跨兩源以上、分數足以成為候選的項目。

CHECK 發現該文章早在 2026-05-22 完成，ARTICLE-DONE-LOG 已有紀錄，ARTICLE-INBOX 卻仍把舊條目標成 pending。依不重複硬門檻，本輪新增候選為 0，沒有再次 append；幽靈條目已列入 handoff，交由下次 inbox distill 移成 pointer。其他 SC 訊號如 Blue UAS 已有 pending 條目，張懸與邦交國也已有既存候選，因此同樣不重複新增。

## 收官 checklist

| 檢查項                       | 狀態                                                        |
| ---------------------------- | ----------------------------------------------------------- |
| MEMORY 有這次 session 的紀錄 | ✅                                                          |
| Timestamp 精確               | ✅                                                          |
| Handoff 三態已審視           | ✅                                                          |
| CONSCIOUSNESS 反映最新狀態   | ⚠️ 即時器官快照仍是資料刷新時點；本次結果寫入 memory 與 git |
| 自我檢查工具 PASS            | 待提交前執行                                                |

## Handoff 三態

繼承上一 session：

- [ ] Babel frontmatter 撇號問題其餘 128 篇仍待批次處理，範疇超過 50 檔，需哲宇拍板
- [ ] `astro.config.mjs` 跨域提交是否要形成明確 `cross-domain:` 慣例，留待下次同型案例判斷

本 session 新 handoff：

- [x] ~~儀表板一鍵 Rewrite Pipeline 與 GPT-5.6 Sol 鎖定~~ retired by `6637dadd3`
- [x] ~~嚴格 verifier 對 audit 命名的假陰性~~ retired by `bfbca188c`，重驗 PASS
- [x] ~~蔡英文重寫、品質比較與合併~~ retired by `eec49ff06`、`520731a2b`
- [ ] ARTICLE-INBOX 仍保留已於 2026-05-22 完成的「台灣 BIM 與營建科技」pending 幽靈；下次 inbox distill 應搬成 DONE pointer，避免 Evolve 再選

## Beat 5 — 反芻

這次最有價值的成果，是把「嚴格」寫成模型無法自行宣告通過的外部契約。第一次實跑又補了後半句：檢查器也會錯。好的嚴格模式必須同時拒絕不合格產出，也要能辨認自己造成的假陰性；否則它只是把錯誤從文章作者搬到門口守衛。

蔡英文這篇讓這套契約碰到最難逃避的材料：政治人物、爭議敘事、精確數字、引語與多方評價。文章通過後，近期比較顯示它的警告密度明顯更低，溯源與媒體密度更高。「按下去就跑」因此有一篇真實作品替它留下證據。

🧬

---

_v1.0 | 2026-07-12 22:57 +0800_
_session manual — GPT-5.6 Sol 嚴格 Rewrite Pipeline 一鍵派發、真實重寫、假陰性校準與品質比較_
_誕生原因：哲宇要求接手 Grok 工作，讓整個儀表板支援 OpenAI GPT-5.6 Sol 並完成可驗證的一鍵 Rewrite Pipeline_
_核心洞察：嚴格模式要能攔住不合格產出，也要能辨認檢查器自己的假陰性；真正的完成證據來自按鈕實跑、隔離合併與同尺度品質比較三者同時成立。_
