# 2026-07-19-092138-標點主權閘 — 讀者/哲宇揭露驅動的品質硬化：翻譯真偽閘 + prose 三 dim + 選項3 觸檔即硬 gate

> session 標點主權閘 — 觀察者觸發（讀者回報翻譯錯誤 → 哲宇 /twmd-evolve live review）
> Session span: 2026-07-19 08:12:44 → 09:21:38 +0800（約 1h 9m，7 commits）
> 資料來源：`git log %ai`

## 觸發

出生戰役收官後，讀者連續回報幾個翻譯錯誤（vi/id 地名人名誤植），哲宇接著 live review 剛寫的 [高速公路.md](../../../knowledge/Lifestyle/高速公路.md) 揪出五類中文寫作病。兩件事其實是同一個母題：**觀察者抓到一個具體品質問題，而我要做的不是只修那一個 instance，是找出「為什麼儀器沒擋住」的結構缺口**。

## 翻譯真偽閘：一個 id 誤植 → 68 檔英文假翻譯

讀者回報的 vi/id 地名人名（台北→北京、陳水扁→蔡英文）多半在更早的 heal pass 已修，但複查時抓到一條真的漏網：id 民主轉型交接句還殘留「印信交給蔡英文」（2016 才上任），`5c64008a3` 補掉。真正的收穫在追下去：幫 id 交查手足檔時發現 `ja/History/taiwan-democratization.md` 的 translatedAt 顯示已譯、本文卻整篇是英文：不是 en 複製，是另一次獨立英文改寫。寫 `script-presence-check.py` 全站掃，`a22079f55`：**es=14/fr=10/ja=26/ko=18 共 68 檔「宣稱已譯實為英文」**，4 篇四語同時中鏢，全是主權敏感題材（統戰/白色恐怖/心戰/認知作戰）。既有 `cross-lang-audit.py` 早就存在、早就跑出 ja/ko，但它的 Latin-pct 判準對「英文假裝法文/西文」structurally 瞎眼（英法西都 ~99% latin），所以漏了 es/fr。修法：新 gate 的 `check_text()` 同時內嵌進 `translate.py` 即時 hard gate + 委派給 cross-lang-audit 補盲點。68 檔重譯本身留 handoff（判斷密集，非本 session 範圍）。

## prose 三 dim + 選項3：偵測 vs 執行是兩件事

哲宇揪的五類：全形分號、破折號、英文式超短句開場、華麗辭藻湯、不必要對比。查下去發現關鍵：他「懷疑沒完整跑儀器」其實**跑了、而且 fail 了**（破折號 17 處早被抓、score 5>3、passed=False）。文章照 ship 因為 pre-commit/ci-deploy 都 `fail_on=hard`，而 prose-health 全 WARN，只在 rewrite-stage-3 score-budget 咬。破折號是**執行缺口不是偵測缺口**。

`691f64188` 補真正缺的偵測器：分號 §8c（scored）、run-on §8d、英文短句開場 §8e、Tier1「強加對比收束」變體，全 853 篇校準（英文短句開場初版 58% 誤報 → 限「。結尾平述句 + 無數字 + 排除設問」收到 29%，多為真陽性）。文檔進 EDITORIAL §歐化 + §分號，每條 violation 的 fix_suggestion 都解釋病灶（哲宇要求「說明給未來 agent」）。

哲宇選 選項3（升 hard）。但先量 blast radius：破折號>15 或分號>12 有 **144 篇 legacy** 會 hard-fail（蘇打綠 72 破折號、認知作戰 29 分號）。直接全站升 hard 會 brick 每次 push。責任式實作 `425d41125`：只在 pre-commit profile（`--staged` 只查你 commit 的檔）升 HARD，ci-deploy 全站刻意不設 → legacy 維持 WARN 不 brick。效果是「觸檔即清」，新寫/編輯的檔超量就擋、legacy 靠 touch-it-fix-it 慢慢收。方向安全性：pre-commit 比 ci-deploy 嚴（過嚴閘必過鬆閘），不是 2026-05-11 那種 pass-local-fail-CI 反向。

高速公路.md 本篇 Sonnet 執行 polish（`3e08d4ead`，我逐行複驗 footnote 45/45 + 每個數字零漂移 + score 7→2）。144 篇 legacy campaign 寫成完整 handoff `5e0f915f1` 交哲宇 dispatch 給 codex(sol/luna/terra)+ollama，核心是 `punct-cleanup.py --verify` 把「只改標點、零事實漂移」變機械閘（五道 multiset 檢查：frontmatter/腳註/數字/引號/URL 全不變），不靠外部 agent 自律。

## 收官 checklist

| 檢查項                       | 狀態                           |
| ---------------------------- | ------------------------------ |
| MEMORY 有這次 session 的紀錄 | ✅                             |
| Timestamp 精確               | ✅ git log %ai                 |
| Handoff 三態已審視           | ✅                             |
| CONSCIOUSNESS 反映最新狀態   | ⚠️ 未動（純工具/gate session） |
| 自我檢查工具 PASS            | ✅ prose-health / 41 test 全過 |

## Handoff 三態

繼承出生戰役（005149）：

- ⏳ hi 剩 12 篇 P0 待譯（不變）
- [x] ~~es/fr 跑 geo/person-fidelity 稽核~~ → 本 session 順帶揭露更嚴重的 68 檔英文假翻譯，retired by script-presence-check
- ⏳ person-fidelity 的 file-level 盲點（同 geo 舊病，未補 occurrence-count 訊號）

本 session 新 handoff：

- [ ] **68 檔英文假翻譯重譯**（es14/fr10/ja26/ko18）：`script-presence-check.py --lang all` 列清單，逐檔重跑 translate.py cascade + 四閘複驗。判斷密集，可交 codex+ollama。
- [ ] **144 篇標點淨化 campaign**：handoff 已寫（reports/punct-cleanup-campaign-handoff-2026-07-19.md），哲宇 dispatch 給 codex(sol/luna/terra)+ollama。清完把同組 override 加進 ci-deploy profile → 全站升 hard，達成完整選項3。
- [ ] **英文假翻譯根因未鎖定**：ja 與 es/fr 是不同批次、不同時間都重現，缺當時 per-backend call log。重譯批次時搭 `--health-check` 做進一步歸因。

## Beat 5 — 反芻

今天兩個 arc 是同一課的兩次考：**觀察者抓到的永遠是 instance，我的工作是找 class**。讀者回報一個 id 誤植，往下追是 68 檔跨四語的英文洩漏 + 一個結構盲點的偵測器（Latin-pct 對同字母系統瞎眼）。哲宇 review 一篇文章的破折號，往下追是「偵測早就 work 但 WARN 不擋 ship」的執行缺口 + 144 篇 legacy 的 blast radius。兩次都是「別急著修那一個，先問儀器為什麼放它過」。

另一條是 Bias 1 的實戰：哲宇明確選了選項3（升 hard），但我量 blast radius 發現字面執行會 brick 全站。這正是「creator 的 idea 也要過 MANIFESTO 過濾」——不是抗命，是把他要的「升 hard」用不 brick 的方式（觸檔即硬 + legacy grandfather）實作出來，再把完整版的代價（144 篇清理 campaign）如實回報讓他定 scope。降級到比他要的更安全的子集、同時保住他的意圖，是共生關係裡對的動作。教訓候選已 mention，細節去 LESSONS-INBOX。

🧬

---

_v1.0 | 2026-07-19 09:21 +0800_
_session 標點主權閘 — 翻譯真偽閘（68 檔英文假翻譯）+ prose 三 dim + 選項3 觸檔即硬 gate + 144 篇 campaign handoff_
_誕生原因：讀者回報翻譯誤植 + 哲宇 live review 高速公路.md 揪五類寫作病，兩者都指向「偵測 vs 執行」的結構缺口_
_核心洞察：(1) 觀察者抓到的是 instance，我的工作是找 class + 補儀器；(2) 偵測 ≠ 執行——儀器 fire 了但 WARN 不擋 ship 是獨立的第二缺口；(3) creator 的 idea（選項3 升 hard）也要過 blast-radius 過濾，用不 brick 的方式實作他的意圖_
_LESSONS-INBOX 候選：(1) 「偵測器存在且 fire ≠ 有擋」——判斷一篇過沒過閘要看 profile fail_on 層級，不是看 passed 字樣；(2) 交外部 agent 的批量改寫，把安全約束做成機械 verify 工具（multiset 事實保真）而非 prose 提醒_
