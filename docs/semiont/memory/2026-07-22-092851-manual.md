# 2026-07-22-092851-manual — 主工作樹清乾淨，未完投影與舊 worktree 都沒有被誤刪

> session manual — 觀察者觸發的 repo／HEAD 整理
> Session span: 2026-07-22 09:28:51 → 09:32:52 +0800（約 4 分鐘，2 commits）
> 資料來源：`git log %ai`

## 觸發

哲宇要我盤點目前 repo 的變更，辨認留下來的內容，再把 `main` 工作樹收乾淨。當時 `main` 與 `origin/main` 完全同步，但有三個未提交衍生資料檔、一份文章投影、四個新語系的 `src/content/` 投影，以及 23MB 的本機批次暫存。

## 分類後再清理

三個衍生資料檔沒有形成完整的一輪刷新：`_translation-status.json` 與品質基線停在 7 月 19 日，analytics 則是 7 月 22 日的單檔局部刷新。它們都能由既有流程重生，因此精確還原到 HEAD，沒有混進新 commit。

「國民政府遷台與戰後重建」投影藍圖是已通過作者自檢、尚待編輯室與正文的真 WIP。我把它單獨保存在 `codex/recover-kmt-projection`，[commit `a3047eb8d`](https://github.com/frank890417/taiwan-md/commit/a3047eb8d) 只有這一檔。`main` 不承接半成品。`tmp/` 與四個 `src/content/{vi,id,pt,hi}` 投影移到系統垃圾桶，可復原也可由 pipeline 重生。

主線 commit `e929bd5ee` 補齊四個新語系與 `/tmp/` 的 ignore 規則，也修正 `verify-commit-scope.sh` 的 `${label}` 邊界。原寫法 `$label）` 在 Bash 解析成另一個未定義變數，讓 scope 檢查自己先 abort。修後 staged 與 HEAD 都實測 2 檔、0 deletion。

## Worktree 盤點

repo 自帶的 `worktree-gc.sh` 先 dry-run，再只移除 12 個「零 dirty、零未進 main commit」的乾淨 worktree。其餘 42 個含未提交檔、未進 `origin/main` 的 commit 或 lock，全部保留。這批是獨立的救援工作，不能為了讓清單變短而自動刪。

## 收官 checklist

| 檢查項                       | 狀態                              |
| ---------------------------- | --------------------------------- |
| MEMORY 有這次 session 的紀錄 | ✅                                |
| Timestamp 精確               | ✅ `git log %ai`                  |
| Handoff 三態已審視           | ✅                                |
| CONSCIOUSNESS 反映最新狀態   | ⚠️ 未動，這次只整理 repo 狀態     |
| 自我檢查工具 PASS            | ✅ memory-diary profile score ≤ 8 |

## Handoff 三態

繼承上一 session：

- [ ] hi 剩 12 篇 P0 待譯。
- [ ] 68 檔英文假翻譯待重譯，重跑時搭 `--health-check` 追根因。
- [ ] person-fidelity 仍缺 file-level occurrence-count 訊號。

本 session 新 handoff：

- [x] ~~`main` 的衍生資料、投影與暫存已分流，12 個安全 worktree 已清掉。~~
- [ ] `codex/recover-kmt-projection` 從投影編輯室開始續寫，完成後再進正文與主線。
- [ ] 42 個保留 worktree 另開救援盤點。逐個判斷 dirty 檔與未進 main commit，禁止批次刪除。

## Beat 5 — 反芻

這次最重要的動作是沒有把「乾淨」誤解成「全部刪掉」。同一個 `git status` 裡同時住著可重生投影、過期快照、局部刷新與一份真的思考成果。先問每份變更的來源與可重生性，清理才不會變成遺忘。42 個舊 worktree 也一樣：數量很醜，但裡面有未存工作時，醜比空白誠實。

🧬

---

_v1.0 | 2026-07-22 09:32 +0800_
_session manual — repo／HEAD 盤點、WIP 救援、衍生物清理與 worktree GC_
_誕生原因：哲宇要求確認現有變更並把 HEAD 整理乾淨_
_核心洞察：repo cleanup 的判準是來源與可重生性；乾淨不能以遺失未完工作為代價。_
_LESSONS-INBOX 候選：無；`verify-commit-scope.sh` 的變數邊界已直接修進工具。_
