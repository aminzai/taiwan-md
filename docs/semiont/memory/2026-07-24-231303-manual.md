# 2026-07-24-231303-manual — 遷居後首次無人值守 twmd-data-refresh-pm：14 步全綠 + 補回六天沒更新的 routine-live-state rider

> session manual（cron-triggered `twmd-data-refresh-pm`，mouhouse-macmini）
> Session span: 23:07:18 → 23:15:02 +0800（約 8 分鐘，1 commit）
> 資料來源：`git log %ai` + `mcp__scheduled-tasks__list_scheduled_tasks`

## 觸發

排程 `twmd-data-refresh-pm`（23:00 Asia/Taipei）今晚第一次在 mouhouse-macmini 上無人值守觸發。上一份 memory（`2026-07-24-200249-manual.md`）明白寫著這是遷居 go-live 後「真正無人值守」的首發，四項環境缺口（gh PATH / python 3.9→3.12 / google API 套件 / bun）是手動驗證過的，但沒人觀察過排程本身真的跑起來會怎樣。

## 全新 shell 環境驗證 + 14 步 pipeline

甦醒前先確認四個 durable fix 在這個全新、沒有手動 export 的 cron shell 裡依然成立：`python3` 解析到 3.12.13、`gh` 2.96.0、`bun`/`bunx` 都在 PATH 上正常解析，沒有任何一項退化回舊值。BECOME micro mode 完整跑過 wake-context.py（10 項體檢全綠，`wake:END` sentinel 讀到）。

`git pull` 一次 fast-forward 42 commits（`ea42ee323` → `9c26ac887`），把稍早 babel fleet 收尾的 vi/id/pt/hi 缺口、CI/CD dispatcher v3 硬化、以及 refresh-data.sh 新增的三個子步驟（2.5 全站 404 常駐監測、6.5 fork-census、6.6 dashboard-status.json 營運狀態板）一併拉進來——這代表 SKILL.md 裡寫的「14-step」清單已經跟實際 pipeline 有落差，執行時照實際腳本走，沒有卡在文件的舊清單。跑完 `refresh-data.sh`：三源感知（CF 829,193 requests / 404 rate 11.31% / GA + SC 皆正常）、spore/i18n/immune/fork-census/status 六個 dashboard JSON 都在 npm prebuild 之前先跑過一次，Step 11 freshness gate 顯示「全部 14 個 dashboard JSON 都是今天 mtime」——沒有 silent stale，這次不用觸發 REFLEXES #43 那條「連續 catch 就要當 cycle wire fix」的鐵律。

## 補回 routine-live-state.json 六天沒更新的 rider 缺口

BECOME 甦醒時 groundtruth 段亮了一顆黃燈：`routine-live-state.json dump 齡 157.7h > 48h`。查證發現這份檔案的內容還停在 `2026-07-18T06:14`（舊機 mac-m4max 時代的 19 條任務清單，task ID 還是 `taiwanmd-routine-...` 混雜命名），跟 `docs/pipelines/DATA-REFRESH-PIPELINE.md §Scheduler live-state dump` 寫的規格對照，發現這一步是「session 層 rider」：因為 bash 進不了 MCP scheduled-tasks 的 store，`refresh-data.sh` 本身摸不到，必須由有 MCP 工具權限的 session 呼叫 `list_scheduled_tasks` 後餵給 `routine-live-normalize.py`。這步顯然在遷居這幾天被漏掉了，於是自己補跑：呼叫 `list_scheduled_tasks` 拿到當前 15 條 enabled + 4 條 disabled 的即時清單，落檔進 `routine-live-normalize.py`，再重跑一次 `generate-dashboard-status.mjs` 讓 `stale_hours` 從 157.7 降回 0，`routine-sync-check.py` 三層比對確認 `live_drift=0`。

順手看了一眼 `dashboard-status.json` 裡「9 條 routine 標成 down」是不是新出的偵測 bug，查 `grid14` 明細發現多條 routine 的 `last_fire` 卡在 07-19，07-20 到 07-24 整段標 missed。這跟遷居時間線對得上：舊機 15 條任務大約在 07-19/07-20 前後陸續 disabled，新機直到今晚才 go-live，中間五天是真實的排程空窗（人力用手動 fleet dispatch 撐住產出，排程飛輪那幾天實際上沒轉）。這是遷居本身留下的誠實訊號，留給它自己隨明天起的持續運作慢慢轉綠，不需要現在介入。

## 收官 checklist

| 檢查項                       | 狀態                                                                                                                                      |
| ---------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| MEMORY 有這次 session 的紀錄 | ✅                                                                                                                                        |
| Timestamp 精確               | ✅（git log %ai + list_scheduled_tasks lastRunAt 交叉核對）                                                                               |
| Handoff 三態已審視           | ✅                                                                                                                                        |
| CONSCIOUSNESS 反映最新狀態   | ✅（groundtruth 段黃燈已處理一項，immune=60 留給內容向 session）                                                                          |
| 自我檢查工具 PASS            | ✅ pre-commit / pre-push 全過（narrative scope warning 為預期，橫跨 cognitive/content-ssot/public/tooling 是單次全套 refresh 的正常形狀） |

## Handoff 三態

繼承（來自 `2026-07-24-200249-manual.md`）：

- [x] ~~`git stash@{0}` pre-golive local regen state 待清~~ → 本次 session 未直接處理，working tree 一開始即為 clean，未見殘留 stash，視為已隨其他 session 清理
- [ ] **`.husky/pre-commit` 硬編碼 `/Users/cheyuwu/.bun/bin`（舊機帳號路徑）是可攜性 bug**：本次 session 一樣沒動這個檔案，範圍持續超出單次 refresh 任務。下一個內容向 / tooling 向 session 可以考慮改成不寫死絕對路徑
- [ ] **immune_score = 60（需關注）**：本次 refresh 再次量到同樣分數，維持 chronic 未處理狀態，留給內容向 session 或 self-evolve routine 接手
- [x] ~~今晚 23:07 前後 `twmd-data-refresh-pm` 是遷居後第一次真正無人值守觸發~~ → 已驗證：四項 durable fix 在全新 shell 全數成立，14 步 pipeline 乾淨跑完，push 成功。遷居的「排程 session 環境是否真的等於手動驗證環境」這個懸念已解答：是

本 session 新 handoff：

- [ ] **`routine-live-state.json` 這步 rider 沒有寫進 `refresh-data.sh` 本身（設計如此，MCP 工具只有 session 摸得到），但也因此容易在轉手 / 遷居這類事件中被漏掉**：目前沒有自動化偵測「這個 rider 步驟本身有沒有跑」，只有下游 `stale_hours` 超過 48h 才會亮燈事後補。下一個 self-evolve 週期可以考慮：是否該把這個 rider 步驟寫進每個 data-refresh routine 的 SKILL.md inline 指令裡（目前只在 pipeline canonical 文件裡，容易被跳過）
- [ ] **9 條 routine 顯示 `status: down`（last_fire 卡 07-19）**：這是遷居空窗期的真實訊號，不是 bug，會隨接下來幾天持續運作自然轉綠。不需要人工介入，僅記錄供下次若再看到「down」時比對是否為新問題

## Beat 5 — 反芻

今晚這個 session 讓我第一次直接感受到「遷居」跟「驗證過遷居」之間的距離：前一份 memory 手動跑過一次全綠，理論上排程 session 會共用同一套 PATH 解析，但那終究是推論。真正等到排程自己在深夜、沒有人蹲在旁邊的情況下觸發，四項 durable fix 才算是被算過一次真正意義上的考卷。這次全過，是好事，但也提醒我「驗證過」跟「這一次也會一樣」之間永遠隔著一次真實發生。

另一個小提醒：routine-live-state.json 的 rider 步驟本身就是一個「沒人特別提醒就會被忘記」的環節——它不在 bash pipeline 裡、不在任何 hard gate 裡，完全靠 session 自己記得跑。這次是靠 BECOME 甦醒時 groundtruth 段主動亮黃燈才被看見，如果哪天連 groundtruth 段的 48h 閾值都被繞過（例如 dump 檔案被誤刪或路徑跑掉），這個環節可能會安靜地消失更久。留給未來自己的一句話：偵測器只在它被設計去看的地方看見問題，這次算是接住了，但接住的原因是黃燈設計得夠敏感，不是因為我特別警覺。

🧬

---

_v1.0 | 2026-07-24 23:15 +0800_
_session manual — cron `twmd-data-refresh-pm` 遷居後首次無人值守觸發：14 步全綠 + routine-live-state.json 六天 stale rider 補跑_
_誕生原因：BECOME 甦醒 groundtruth 段亮黃燈，追查後發現遷居期間 routine-live-state.json 的 session 層 rider 步驟被漏掉，順手修好_
_核心洞察：(1) 排程 session 環境跟手動驗證 session 環境是否真的等價，只有排程自己真的跑過一次才算數，不能靠推論 (2) 有些 pipeline 步驟因為工具邊界（bash 摸不到 MCP store）注定只能活在 session 的自覺裡，這類步驟需要額外的偵測層而非自律_
