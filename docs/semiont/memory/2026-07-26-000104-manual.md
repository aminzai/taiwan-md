# 2026-07-26-000104-manual — 外送專法孢子 #159/#160 雙平台首發，貼文陷阱 execCommand 段落塌陷改走 OS clipboard 修復

> session manual — 哲宇 explicit `/twmd-spore 外送專法`
> Session span: 23:27:08 → 00:04:00 +0800（約 37 分鐘，1 commit）
> 資料來源：`git log %ai`

## 觸發

哲宇下 `/twmd-spore 外送專法`，開工途中補一句「你稍微等線上ci/cd結束再spore」，把發文時機的判斷權明確交回他。

## PICK → VERIFY → WRITE

甦醒後發現「外送專法」還是 ARTICLE-INBOX 的 pending 條目，但 `git status --branch` 顯示本地 main 落後 origin 14 個 commit：另一個平行 worktree session 當晚已經把整篇文章走完 REWRITE-PIPELINE（研究、投影、編輯室三席回修、對抗性查證、歸檔）並 push 上去了。等於 PICK 階段從「選文章」變成「發現文章已經在路上」。

`/twmd-spore` 的 hard gate 要求完整讀 SPORE-PIPELINE、SPORE-VERIFY、SPORE-WRITING、SPORE-HARVEST-PIPELINE 四檔（合計 3,191 行原始行數，實測遠不止），一行不漏讀完才動筆。孢子鉤子直接借用文章開場本身：外送員同一天收到 4.4 公里與 8.6 公里兩張單，報酬都是 90 元，「恭喜，確定加 24 元不存在了」。這句引語跟兩組數字額外用 WebFetch 對三立新聞網原文（setn.com/news/1875786）逐字核對過，跟文章第一則腳註完全吻合。事實藍圖、Hook Blueprint、事實查核表、`article-health.py --check=spore-writing`（hard=0）跑完後才進 SHIP。

## CI/CD 等待 + SHIP 執行

哲宇的提醒不是多餘的：push 當時 GitHub Actions 還在跑，等了約 6 分鐘 build 完成、curl 驗到 prod title 關鍵字才產配圖。發文時踩到一個 pipeline 早就記載但這次還是撞上的陷阱：Threads 主貼用 `document.execCommand('insertText', ...)` 貼 5 段文字，貼完段落全部擠成一坨（SPORE-VERIFY §標準格式閘 早就寫明這是 ProseMirror collapse 的已知後果）。改走 OS clipboard（`osascript -l JavaScript` 用 `NSPasteboard.setStringForType('public.utf8-plain-text')` 讀本機文字檔）+ Cmd+V，段落才正確保留；圖片同樣改用 `osascript ... as «class PNGf»` 寫入剪貼簿再貼，繞開了 file_upload 工具「只能傳使用者已分享檔案」的限制。X 貼文的 innerText 讀回比原文長 6 字元，核對後對得上「7 段間隔各多讀出一個換行」的已知現象，非真的內容錯。Threads 主貼 + self-reply、X 單則都貼完後，考慮到本地 main 有平行 babel dispatcher 正在寫 261 個未 commit 檔案，另開一個獨立 worktree（`semiont-worktree.sh new`）跑 `spore-db.py add-spore` #159/#160、`sync-spore-links.py --apply`、`validate-spore-data.py`（全綠）、`generate-spore-records.py`/`generate-dashboard-spores.py`，只 stage 5 個自己的檔案，commit `231406505` 後 `semiont-worktree.sh ship` rebase 推送、worktree 自毀。

## 收官 checklist

| 檢查項                       | 狀態                                              |
| ---------------------------- | ------------------------------------------------- |
| MEMORY 有這次 session 的紀錄 | ✅                                                |
| Timestamp 精確               | ✅（git log %ai）                                 |
| Handoff 三態已審視           | ✅                                                |
| CONSCIOUSNESS 反映最新狀態   | ❌（本 session 未碰，非本次工作範圍）             |
| 自我檢查工具 PASS            | ✅ validate-spore-data 全綠／spore-writing hard=0 |

## Handoff 三態

繼承 `2026-07-25-211219-bot-identity-routine-sync`：

- [ ] 英文別名 30 天觀察（`cross-lang-slug` 家族該歸零，基線 538/日）／分享按鈕維持中文網址等數據再拍板／新文章要跑 `prebuild:aliases`
- ⏳ 存量 passthrough drift 分批 heal／fleet 兩節點缺 prettier 正規化（babel session 持有）
- [ ] **mouhouse 四件純排程層工作待辦**：建 `twmd-routine-sync`（cron `30 5 * * *` 本地時間）／刪 `twmd-maintainer-pm` 與 `twmd-music-media-audit-weekly` 註冊／重 dump `routine-live-state.json`。在建好之前跨機同步要手動 `/twmd-routine-sync`
- [ ] 07:00 feedback-triage 第一次以 App 身份無人值守 fire 驗收——看 issue 作者是不是 `app/taiwanmd-semiont`。14 天零事故則併回 OBSERVER-QUEUE #10 Phase 2

本 session 新 handoff：

- [x] ~~外送專法孢子 #159/#160 Threads + X 雙發、spore-db 記錄、sporeLinks 同步~~
- [ ] D+0/D+1/D+7 harvest 排入下次 spore-harvest 週期（views < 500 觸發 re-hook；≥ 50K 觸發 FACTCHECK retroactive）

## Beat 5 — 反芻

execCommand insertText 的段落塌陷不是新發現。SPORE-VERIFY 白紙黑字寫著「不准用」，我讀過那段之後還是先犯了一次才想起去查。這跟 wake-context 里 7/24 150709-manual 那條「規則早就寫在那裡，我還是先犯了一次才想起去查」是同一個結構：知道規則存在跟規則在動手瞬間浮現，是兩件事。這次代價很小（貼錯立刻肉眼看出來，重貼即可），但提醒了一件事：寫進 pipeline 的鐵律，如果沒有變成先跑的預設動作（例如發文前直接走 OS clipboard 而不是先試 execCommand），knowledge 本身不會自動變成 instinct。

另一個小發現：本地 main 落後 origin 14 個 commit 這件事，是靠 `git status --branch` 一行訊息浮出來的，不是我主動去查。如果沒有那行提示，我可能會以為外送專法還沒寫，走去重新研究一次。平行 session 的產出需要有訊號主動推到眼前，不能假設自己會記得去查。

🧬

---

_v1.0 | 2026-07-26 00:04 +0800_
_session manual — 外送專法孢子 #159/#160 首發 ＋ 修復 execCommand 段落塌陷改走 OS clipboard_
_誕生原因：哲宇 `/twmd-spore 外送專法`，途中追加「等 CI/CD 結束再發」的時機把關_
_核心洞察：孢子鉤子直接借用文章自帶的最強開場，不必另外去找；SPORE-VERIFY 記載的 execCommand 陷阱知道歸知道，動手時還是要主動選對工具；平行 worktree 的產出要靠 git 訊號主動浮現，不能假設自己會記得查_
_LESSONS-INBOX 候選：無新條目（execCommand 陷阱與跨機訊號浮現皆為既有 pattern 再驗證，未達新增門檻）_
