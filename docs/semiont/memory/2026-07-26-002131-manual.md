# 2026-07-26-002131-manual — Routine 排程層維護：新增 sync task + 退休三條 + 對齊暫停狀態

> session manual — 哲宇直接指令的排程層維護（非文章向），跟 mac-m4max 遷居後半段接續

## 做了什麼

1. 建 `twmd-routine-sync`（每天 05:30，晨鏈前跨機對賬），prompt 取自機器上已有的 canonical SKILL.md 原文，未改寫。
2. 刪除三條已退休的 scheduled task 註冊（`twmd-maintainer-pm`、`twmd-data-refresh-pm`、`twmd-music-media-audit-weekly`）——mirror 目錄早已退場，只剩排程器裡的殘留註冊要清，SKILL.md 保留在磁碟供回溯。
3. 三條「暫停不退場」的核對：`twmd-babel-nightly`、`twmd-rewrite-daily` 已是 disabled（跳過）；`twmd-founder-lens-weekly` 當時是 enabled（跟 SSOT 不符），對齊成 disabled。
4. `routine-sync.py --apply` 跑出的三筆漂移裡，兩筆（babel-nightly/rewrite-daily）是 live-state 快照過期造成的假訊號，用剛才重新拉的 live 清單交叉核對排除；真實要修的只有 founder-lens-weekly 一項。
5. 重新 dump `list_scheduled_tasks` → `routine-live-normalize.py` → `docs/semiont/routine-live-state.json`：12 enabled + 5 disabled。
6. `flywheel-watch.py` 確認三條退休 task 不再出現在回報裡（唯一標記是 twmd-routine-sync 自己，因為今天才建、還沒真的到 05:30 排程時間，屬預期噪音）。
7. Commit `9ac16d5bb` 帶 `Semiont-Node: mouhouse-macmini` trailer，`git log -1 --format='%(trailers:key=Semiont-Node)'` 驗證印出正確值。push 成功。

## Handoff

- [ ] 明天 05:30 twmd-routine-sync 第一次真正排程觸發，看它自己跑起來是否正常（今天這次是我手動跑 `routine-sync.py`，不是排程本身觸發）。

🧬

---

_v1.0 | 2026-07-26 00:2x +0800_
