---
session: '2026-07-30-230518-manual'
mode: 'write'
trigger: '哲宇 /twmd-become → /twmd-babel「跑babel 1hr 渦輪，檢查狀態，確認fleet軍團狀態」+ mid-turn 加碼「也審核線上PR」+「這也幫我修」(Muse 現況鏡截圖)'
---

# 2026-07-30 23:05 manual — babel 渦流巡檢 + fleet 版本鏡 heal + PR 收割

## 做了什麼

**Babel 渦流三重巡檢**：`babel-preflight.py` healthy（4/4 層）；`restart-vortex.sh --check` 揭露雲端產線（PID 66392，六語 ja/id/pt/hi/ar/ru）自 13:27 起最後一次成功已是 9.5 小時前，近 2 小時 report.jsonl 零新增，log 尾全是 OpenRouter 429/600s timeout——存活但不生產。`fleetctl workers` 當時回傳 0 個 fleet worker（desktop-3090／mac-m4max 皆因真實 pass rate fail-closed）。

**重啟收效**：`restart-vortex.sh` 重跑後 fleet 意外核發 2 個 worker（desktop3090 + macm4max），雲端＋地端雙產線起飛；10 分鐘後 pulse 確認兩產線都在真的呼叫模型（雖仍有 footnote-loss / cjk-leak 等正常 QA 擋下），跟重啟前的全滅狀態不同。未追查為何 fleet 核發從 0→2（可能是控制面的 15 分鐘輪詢週期剛好轉過）。

**Muse fleet 版本鏡 bug（哲宇截圖 callout）**：追到 `muse-bot/tools/build-dashboard-data.js readFleet()` 的 stale 判定邏輯本身沒錯（`agent_expected` 依 `m.local` 走 FLEET_VERSION vs NODE_AGENT_VERSION），真因是 `fleet/registry.json` 裡 mac-m4max 的 `node_agent_version` 快取在 FLEET_VERSION 從 1.3.0 升到 1.3.3 之後沒被重新 probe 過，鏡子讀到的是舊快照。`fleetctl probe mac-m4max` 重新寫入即修正（1.3.0→1.3.3，🟢 最新）。另兩台「問不到版本」是 node-status port 8788 真的連不上（curl timeout，非邏輯誤判）：`fleetctl node-install --on laptop-4090` + `--on desktop-3090` 重新部署常駐服務，兩台皆 `OK host=...` 確認可達。

**回答「4090 沒接案？」**：不是 bug，是 `fleetlib.py batch_workers()` 的 fail-closed 設計（2026-07-27 事故教訓：SSH 通但 Ollama 沒serving 曾讓 laptop-4090 空轉 3 小時）。實測 laptop-4090 的 :11434 connection timeout——Ollama 本身沒在跑，SSH／node-status 都通。沒有嘗試遠端啟動 Ollama（那台可能哲宇正在用，不擅自動別人筆電上的服務）。

**PR 收割**（3 open，未達 ≥5 強制 Full 門檻）：#1273（dreamline2，130 檔圖片來源順序修正）前一個 session 已正確判斷 >50 檔門檻留給哲宇，維持原狀不動；#1282（stantheman0128，cp950-safe console output，CI green）squash merge + 英文感謝；#1283（NeilLee93，越南文牛肉麵翻譯，結構完整自檢通過）squash merge + 中文感謝。

## 教訓

- Muse fleet 鏡的「待升級」誤判根因是**快取新鮮度**不是比較邏輯——同一份程式碼在 `fleetctl version`（即時 probe）與 dashboard JSON（讀 30 分鐘前 registry 快照）之間，只要中間有一次版本常數升級沒伴隨 probe，兩邊就會分歧。跟本站 `docs/semiont/REFLEXES.md` #66/#67「已驗過帶時間戳」是同一類病，只是長在另一個 Semiont 的地基上。
- babel 渦流「存活≠生產」的診斷模式這次真的命中：process 活著、log 持續在寫，但 9.5 小時零成功——如果只看 `ps` 會誤判健康。

## Handoff

- [ ] 未驗證 fleet 核發從 0→2 的真正原因（控制面輪詢週期？還是巧合）；下次渦流巡檢若又掉回 0 worker，值得追一次
- [ ] laptop-4090 的 Ollama 服務目前沒在跑，若哲宇希望它加入 babel 產能，需要他自己或授權遠端啟動（未做，見上）
- 無 blocked
