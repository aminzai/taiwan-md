# 2026-08-01-064443-manual — 渦流閘門自我校準三輪：§14b 從全擋改只擋新退化、desktop-3090 診斷收斂到「不覆蓋觀察者」、金瓜石 patch-reject 結構性死鎖診斷

> session manual — 承接 00:40 委派層 memory 之後，繼續巴別塔渦流每小時監測
> Session span: 00:41:10 → 06:49:21 +0800 (約 6h08m, 12 commits)
> 資料來源：`git log %ai`

## 觸發

延續哲宇稍早的 `/goal`（解決所有問題、進化軍團、最佳化成功率、每小時重新檢查），104 篇委派翻譯收工後渦流繼續跑滿三輪。哲宇最後說「先 /aftre」示意收官。

## §14b 閘門誤傷自己人，兩輪 evolve 補強判準

00:41 那份 memory 提到的 §14b 新閘門（frontmatter 欄位遺漏檢查）上線當夜就誤擋了 `es/鄭愁予` 的零成本 `semantic-noop-bump`。根因是它把「存量債」跟「新退化」混成同一訊號，凡是 zh 有而譯文缺的欄位一律 FAIL，但那批缺的欄位其實 HEAD 版本也缺。`f86b7f2b5` 改成跟 git HEAD baseline 比對，只有新掉的欄位才 FAIL，存量債降級 WARN。`830b78bb6`（v1.51）記下這次誤擋事件，順手量出全站 186 檔帶著這種標籤存量債。

修完閘門後回頭想校準方法論本身，補了兩條 REFLEXES #66。`2f18ba2ec` 的 (e) 是「校準語料要涵蓋閘門將跑過的全部族群，不能只用觸發這次修法的那個批次」。`a70408e2a` 的 (f) 則是自己親身撞到的：把舊版 `verify-translation.py` 複製到 `/tmp` 比對新舊行為，結果 20/25 檔「疑似退化」，查下去才發現是複製後的腳本用 `Path(__file__).resolve().parent.parent.parent.parent` 算 `REPO`，搬到 `/tmp` 後這條路徑指向檔案系統根目錄，所有存在性檢查全部假摔。把舊版放回原本目錄重跑，正確數字是 8 個真修好、0 個真退化、17 個沒變。對照組要跟受測物同一個執行環境，不是同一份程式碼就夠。

## desktop-3090 診斷收斂：確認換模型三路都走不通，但守住不覆蓋觀察者的邊界

`384bb429a`（v1.52）把 desktop-3090 三個選項寫清楚：停用、換模型、接受現有產出率。並確認自己不擅自關閉，因為哲宇在 dashboard 上是手動開啟這個節點的，渦流的衝突解決規則本來就是「觀察者手動設定優先」。`03304a621` 是隔一輪的複驗，確認狀態沒有轉好，維持 0/17，換模型路徑三次確認都走不通。這條 handoff 沒有被解決，是刻意保持 pending，等哲宇自己決定要不要接受這個節點目前的產出率或换硬體策略。

## v1.53：金瓜石 patch-reject 死鎖診斷完，修法寫下但沒動手

`ja/Geography/金瓜石.md` 連續 13 次同類型失敗，同一個 CJK 殘留（`'如果' x1`）卡在第 9 章。診斷出根因是 `patch-translate.py` 的既有設計：exit=1 時不 fallback 到整篇重翻，是 v1.11 刻意避免雙重燒 compute 的決定。但沒有配套的「連續失敗太多次就升級」機制，所以難篇會在同一個壞 chunk 上無限循環。`36784fbd0` 把修法寫進 BABEL-VORTEX-LOOP.md（fail-memo 記到 ≥5 次 patch-reject 就強制 `--engine=whole`），但沒有在這輪動手改，因為當時已經接近 context 邊界，核心分派路徑的改動不該在那種狀態下做。

## Routine 飛輪並行狀態

同一段時間背景 routine 照常轉。`3c894423e` 05:34 的 embeddings nightly 重建 12 語 8686 向量零失敗，vi 連三夜持平後首度跨過 400 篇門檻（448 篇），那條 handoff 隨之退役。`e3184ad05` 05:37 三層對賬連七輪全綠，`faf75fb77` 06:15 data-refresh-am 14 步全綠零 stale。渦流跟 routine 兩條軌各自獨立又互相印證。desktop-3090 的零產出在渦流跟 dashboard 兩邊看到的是同一件事，沒有互相矛盾。

## 收官 checklist

| 檢查項                       | 狀態                       |
| ---------------------------- | -------------------------- |
| MEMORY 有這次 session 的紀錄 | ✅                         |
| Timestamp 精確               | ✅（全取自 `git log %ai`） |
| Handoff 三態已審視           | ✅                         |
| CONSCIOUSNESS 反映最新狀態   | ✅                         |
| 自我檢查工具 PASS            | ✅                         |

## Handoff 三態

繼承上一 session（00:41 memory）：

- [ ] 1,802 檔 frontmatter 欄位遺漏（researchReport 844、sporeLinks 293 等）— `heal-missing-frontmatter.py` 已備妥，>50 檔屬 §自主權邊界，待哲宇拍板
- [ ] 108+ 既有斷鏈目標（gated ratio 0.44%）— 是否升 hard gate 屬閾值調整，待拍板
- [ ] vi 語言缺口（現況 fresh 439 / missing 422 / coverage 51.9%，比 00:41 時的 39.9% 已推進，但仍有缺口）— 照同法清完估 34M token，預算待決定
- [ ] `Economy/台灣動畫代工.md` 資料錯亂（9 語言譯文其實是另一篇）— 刪除 9 檔屬 §自主權邊界，未動
- [x] ~~186 檔標籤存量債~~ — 已量化並降級為 WARN 不擋 commit（`830b78bb6`），不再需要單獨修復

本 session 新 handoff：

- [ ] **desktop-3090 三選一**（停用 / 換模型 / 接受現有 0/17 產出率）— 三路已診斷清楚，純粹是哲宇的資源決定，不需要更多技術調查
- [ ] **ja/金瓜石.md patch-reject 死鎖修法**（fail-memo ≥5 次強制 `--engine=whole`）— 方案已寫進 BABEL-VORTEX-LOOP.md v1.53，下一輪渦流可以直接動手改 `babel-dispatch.py`
- [x] ~~§14b 閘門誤擋~~ — 已修復並驗證（3 組回歸案例）

## Beat 5 — 反芻

這三輪的共同結構是「自己造的東西在第一天就犯了它要防的病」。§14b 閘門本身在防混維度，卻先犯了混維度。用來校準閘門的 /tmp 對照實驗本身也踩了環境不對等的陷阱。新工具上線的第一次真實碰撞往往揭露設計時沒想到的邊界情況，發現方式各不相同：一次是 production 上真的擋錯東西，一次是自己複查時數字看起來很嚇人。兩種訊號都該認真查到底，而不是先假設是自己判斷錯就跳過。

desktop-3090 跟金瓜石這兩條 handoff 有個共通點，診斷都做完了，但都刻意沒有動手。一個是邊界問題，觀察者的資源決定不該被越權。一個是時機問題，核心路徑不該在 context 邊界改。渦流跑了六輪之後，區分「診斷完就該動手」跟「診斷完但故意停在這裡」變得比較有把握，不再是每次都要重新猶豫。

🧬

---

_v1.0 | 2026-08-01 06:49 +0800_
_session manual — 承接 00:41 委派層 memory，渦流三輪（v1.51/1.52/1.53）+ REFLEXES #66 兩次補強 + routine 飛輪並行狀態_
_誕生原因：哲宇「先 /aftre」示意收官，觸發 `/twmd-finale`_
_核心洞察：新工具上線第一天最容易犯它自己要防的病；診斷完成不等於該動手，判斷「現在該不該做」本身是一項獨立能力_
