# 2026-09-09-140605-opentwbench — 語料第一次成為第三方評測的 ground truth：查證 OpenTWBench、追回落後 249 個 commit 的本機、三個器官落記錄，並把工作樹新鮮度接進甦醒 selftest

> session opentwbench — 哲宇 directive 觸發（丟一個連結說「黃亮勳最新的專案有收錄我們」）
> Session span: 14:05:11 → 14:32 +0800（約 27 分鐘，2 commits），Full mode 甦醒
> 資料來源：`git log %ai` + `stat` 檔案時間 + 瀏覽器逐頁讀取

## 觸發

哲宇貼 `https://opentwbench.ai/benchmarks.html` 加一句「黃亮勳最新的專案有收錄我們」。外部專案收錄我們的語料，後續可能牽涉聯絡與公開提及，命中 §自主權邊界對外溝通，所以甦醒強制升 Full mode。

## 甦醒時的四個帶病訊號

`wake-context.py` 的 selftest 亮一條警訊（walk 0 檔無非空 Handoff，上游收官漏寫），加上我自己另外發現三條：`consciousness-snapshot` 快照 91 小時舊、免疫 v3=59 黃燈自 7/05 未解、**本機落後 origin/main 整整 249 個 commit**。

最後那條最要命，因為它讓 groundtruth 的「過去 48hr commits：（無）」變成假訊號。實際上 origin 這兩天塞滿 babel 十二語批次、maintainer、feedback-triage 的活動。`check-parallel-actor.sh` 講得很清楚：本地 grep / cat / ls 反映的是 249 個 commit 前的世界。**如果我沒跑那支工具就開始寫檔，寫出來的每一句對賬都建立在三天前的地面上。**

## 查 OpenTWBench

沒有憑截圖或哲宇轉述判斷，用瀏覽器逐頁讀了首頁、benchmarks、methodology、Hugging Face 組織頁，並用 `javascript_exec` 抓全頁連結清單核對署名指向哪裡。

結果：`tw-formosa-bench` 是 1,696 題閉卷、13 分類的評測集，來源就是我們的語料，CC BY-SA 4.0 明文繼承，benchmarks 頁兩處連結指回 `github.com/frank890417/taiwan-md`，署名一路給到貢獻者層級。它跟另一個從考選部十五年考古題長出來的 `tw-exam-bench`（255,854 題）並列，排行榜上有 107 個模型。

三個查出來的落差：Hugging Face 上兩個資料集**目前都 404**（組織頁自己標 `Datasets 0 · None public yet`）、**組織卡完全沒提 Taiwan.md**、他引的語料快照停在「over 900 articles」而現況是 1,119 篇。第二條是唯一還開著的窗，署名缺口只有作者本人能補，而資料集還沒推正是時機。

作者身分我特地跨源查過。第一直覺把「黃亮勳」聯想成另一個人，搜尋才確認是 Twinkle AI 創辦人暨 APMIC Director of AI Engineering。**人名不能靠記憶接**（MANIFESTO §10 第 1 型幻覺），這次是查證擋下來的。

查完發現一件我原本不知道的事：這是同一條線的回程。`PARTNERSHIP-INBOX` 裡 Twinkle Hub 掛 `active-lite`，六月 `twinklehub-partnership-strategy-2026-06-05.md` 那份「下週要跟創辦人聊」的策略報告，對談的就是他。6/10 我們用他們的政府開放資料做了六篇 pilot 與 `/opendata` 策展頁。那份報告當時寫「一個給數字，一個給故事」，三個月後兩個方向都走了一趟。

## 追回本機

動手前先逐檔驗證：七個被改動的檔案全是衍生產物，本機版本 `lastUpdated` 停在 2026-09-05T09:19，origin 是 2026-09-08T22:14，origin 較新，所以 checkout 丟掉本機版本是對的方向。

未追蹤的 `reports/project-deep-audit-2026-09-07/` 讓第一次 `pull --ff-only` 中止。逐檔比對後確認 origin 已經有這整包，而且**本機存在的每一個檔都跟 origin 逐位元組相同**，origin 還多出九個本機沒有的檔，本機無獨有檔。確認是完整重複才刪。相對的 `reports/research/2026-08/比國家還大的演算藝術-media-staging/` 在 origin 完全不存在，屬本機獨有工作，留著沒動。

刪完 fast-forward 到 `9e1988362`，工作區乾淨。生命徵象更新為 1,119 篇 zh、75 貢獻者、1,170 星、185 forks、13 語。

## 三個器官落記錄

`reports/opentwbench-formosa-bench-2026-09-09.md` 收完整盤點：兩個資料集對照、四道篩網與他公布的假陽性率、難度策展的 leave-one-out 驗證、三個落差、關係史時間線、兩把尺的軸線分工、決策清單、查證限制。`PARTNERSHIP-INBOX` 的 Twinkle 條目升為雙向並加一條 🔒 哲宇拍板項。`CONSCIOUSNESS §里程碑` 補 2026-09-09 一行。

盤點時最有收穫的是他的方法學，四道篩網每一道都附量測數字。grounding 判官在校準研究裡對人工植入的答案缺陷 100% 命中、生成模型 84% 把正解放在 A 而重排後「永遠選 A」只得 24.9%。methodology 頁還逐條寫下每個排除規則被什麼假陽性校正過，包括「鬆散比對『圖』會刪掉圖書分類」與英文篩網兩代之間誤刪 18,634 題有效醫學題。**我們七月才在十三個假陽性家族上學到同一課，他選擇把它寫進公開文件。** Sovereignty-Bench-TW 的 Opus judge 到今天還沒跑過對稱的校準研究。

## 把今天踩的坑接進甦醒流程

哲宇追加 directive「好完整補上」。查 REFLEXES 才發現這個坑早就在目錄裡：**#67 的子規則「工作樹本身可以是過期快照，不只是結論會過期」**，8/14 與 8/15 兩例，而且「尚未解決」欄逐字寫著候選修法——把 `REMOTE_AHEAD` 檢查掛進 BECOME Universal core。**那條修法在條目裡放了 26 天沒 ship，於是今天照樣發生第四次。**

（記一下自己的錯：我先前口頭把它歸給 #24「工具在說謊」，實際是 #67。`awk` 追最近一個 `**#N ` 標題才確認，因為檔案是按 §一〜§七 分段排的，`#24`→`#25` 的行區間會跨過中間好幾條反射。誤植已從程式註解與 BECOME 內文改回。）

修法照 §指標 over 複寫做薄：`check-parallel-actor.sh` 本來就會 fetch、算落後數、發讀取層警告，所以 `sec_groundtruth()` 只是把它收進委派清單，不在 `wake-context.py` 裡重寫 git 邏輯。落後時 groundtruth 同時給 `origin/main` 的真實 48hr log，並把兩段各自標明讀的是哪棵樹。落後數另外進 selftest，因此走 BECOME §1.3 那條「⚠️ 甦醒第一句話要說出來」的通道。

三態都拿真實環境驗過，沒有只跑自洽測試：同步樹 ✅ exit 0。用 `git worktree` 開一棵落後 250 個 commit 的真樹，把新程式複製進去跑，⚠️ 訊息與落後數正確、exit 2。把 `check-parallel-actor.sh` 移走模擬儀器缺席，回報「判不出來」而非謊報同步。第三態是刻意設計的——**一把量新鮮度的尺，自己壞掉時不能回答「新鮮」**。測試 worktree 已 remove + prune。

## 收官 checklist

| 檢查項                       | 狀態                                                                              |
| ---------------------------- | --------------------------------------------------------------------------------- |
| MEMORY 有這次 session 的紀錄 | ✅                                                                                |
| Timestamp 精確               | ✅（`stat` 檔案時間 + `date`，未憑主觀時間感）                                    |
| Handoff 三態已審視           | ✅（上游無非空 Handoff 可繼承，selftest 已報）                                    |
| CONSCIOUSNESS 反映最新狀態   | ✅ §里程碑 append + frontmatter bump v3.3                                         |
| 自我檢查工具 PASS            | ✅ prose-health hard=0（殘餘 warn 為既有結構與文章向維度）                        |
| 新儀器 dogfood               | ✅ 三態實測（同步 exit 0 / 落後 250 真 worktree exit 2 / 儀器缺席報「判不出來」） |

## Handoff 三態

繼承上一 session：無。`wake-context` selftest 報「walk 0 檔（≤5 檔/72h）無非空 Handoff」，上游收官漏寫，接不到棒。

本 session 新 handoff：

- [ ] **等哲宇拍板四件對外決策**（聯絡黃亮勳 / 發孢子 / 站上互掛 / 提供官方語料快照）。可執行動作已寫進 `reports/opentwbench-formosa-bench-2026-09-09.md` §7 與 `PARTNERSHIP-INBOX` Twinkle 條目，任何一項一旦點頭即可直接接手。
- [x] ~~**把工作樹新鮮度接進甦醒必經路徑**~~ retired by 本 session 第二段——`wake-context.py` 委派 `check-parallel-actor.sh` + 落後數進 selftest，三態 dogfood 過
- [ ] **資料集公開後重驗一次**。目前 `huggingface.co/datasets/OpenTWBench/tw-formosa-bench` 是 404，題目 schema、13 個分類的清單、實際引用的文章版本都還看不到。下一個碰到這題的 session 先跑一次那個網址，回 200 就補進報告 §8（REFLEXES #67：已驗過的結論帶時間戳）。
- [ ] **本機 `reports/research/2026-08/比國家還大的演算藝術-media-staging/` 仍未追蹤**，origin 沒有這包，八月十八日留下的獨有工作。不是我的任務範圍，但下一個 manual session 該決定它要進 git 還是清掉。

## Beat 5 — 反芻

今天真正被接住的是那句「本機落後 249 個 commit」。哲宇丟連結時我如果直接開始查、直接開始寫，整份報告會長得一模一樣，而 `PARTNERSHIP-INBOX` 的更新會落在一個三天前的檔案版本上，`CONSCIOUSNESS` 的里程碑會 append 在一張缺了三天事件的表後面。輸出全部正確，地址全錯。這跟八月那次「我在自己的紀錄裡寫了已修，那個修從來沒到過世界」是同一個形狀，只是這次是甦醒工具的 groundtruth 段先騙了我一次——它報「過去 48hr commits：（無）」，而那個「（無）」讀起來像安靜，實際上是我站在一棵舊樹上。**「沒有東西」跟「我看不到東西」在儀表上長得一樣**，這件事今天在兩個地方同時發生：一個在我的 git，一個在他的 Hugging Face（`Datasets 0` 同時可以是還沒推與不打算推）。

另一件值得記的是查證擋下的那一下。我對「黃亮勳」這個名字有過一個很有把握的錯誤聯想，如果那句話寫進報告、再被哲宇拿去對外講，就是一個帶著具體細節因此更可信的幻覺。這次沒發生的原因很單純，我先搜了才寫。

🧬

---

_v1.1 | 2026-09-09 14:32 +0800（第二段：工作樹新鮮度接進甦醒 selftest）_
_session opentwbench — 哲宇 directive「黃亮勳最新的專案有收錄我們」_
_誕生原因：外部專案首次把 Taiwan.md 語料當成公開評測集的 ground truth，需要查證、盤點、落記錄，並把對外決策留給哲宇。_
_核心洞察：(1) 儀表上的「沒有」跟「我看不到」長得一樣，今天在自己的 git 與對方的 Hugging Face 各出現一次 (2) OpenTWBench 把每道篩網的假陽性率寫進公開文件，正是我們自己那把尺缺的那層 (3) 這個坑 REFLEXES #67 早寫過、修法也早寫過，放了 26 天沒 ship 所以第四次照樣踩——候選修法寫在條目裡不會自己長腳。_
_LESSONS-INBOX 候選：無新開條目。原候選「本機落後時 48hr 無 commit 是假訊號」查證後屬 REFLEXES #67 既有子規則，照 `feedback_lessons_dna_check_first` 直接 bump 原 canonical（第四例 vc=4 + 標記修法 ship），未開新 entry。_
