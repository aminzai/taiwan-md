# EZ WAY 局部試稿：v3 受影響裁決

- Reviewer：`article_comparison`（Codex 子代理）
- 日期：2026-09-07
- Context disclosure：已讀研究／藍圖及 v1–v3，非獨立盲讀者、非人類品味評審。
- Scope：局部比較試稿；不核准完整文章或發布。
- v3 SHA-256：`c7f73820654cf6a425fc2c9dca07d0c9a27de40d9126148020a4e1de29955173`
- 先前 compose 之後的接受不沿用；以下是針對 v3 重新判讀，不聲稱已替工具變更狀態。

## compose：accept

`draft-v3.md:3` 的條件語氣「可能出現」消除先前一瞬間像在敘述事後結果的歧義，仍保留署方當時預估的歸屬。L5 改成「提出哪些配套」，符合實際列出的討論與提前推播要求，不再暗示已交代完整倉儲作業。最後仍把等待件數和時間留作未回答問題，沒有將語氣修正變成新政策效果主張。

接受作局部比較稿，理由是流程入口、具體疑問與來源邊界清楚，不是關卡已完成。

## verify：accept（v3 局部來源語意與 Markdown 引用；瀏覽器驗收另待）

語意部分：本輪對照同日先前親自取得的 S1 第二段之條件性倉容評估，以及 S1 末段／S2 第三段之說明會與提前推播內容；取得方法見 v1 審稿。v3 兩項措辭更貼近來源，沒有新增無來源承重事實。本輪沒有重新擷取網頁，不冒稱 fresh fetch。

渲染部分：本人除了讀 `render-check-v3.txt`，另實際以 Node `marked.parse` 讀 v3、抽取所有 `<a href>`，用 assert 檢查恰有三個、標籤依序為 1／1／2、URL 的 cntId 依序為 S1／S1／S2，命令退出碼 **0**。實際輸出如下：

```text
1 → b1bc5147371d428590c00553ba094bd2
1 → b1bc5147371d428590c00553ba094bd2
2 → 5b7a9da7e7674b4e90e0cee314f85191
```

這比 v2 只數目的地更能檢查引用對位。先前 v2 的局部驗收沒有捕捉 `[1][2]` 的 Markdown 語法歧義，屬實際驗收缺口；不能因原文字面上列有兩個編號就認定兩個來源都呈現正確。

未親自檢查 v3 的瀏覽器 DOM 或正式 Astro 文章渲染。此 verify 接受僅涵蓋以上已執行的局部查證；若工具將 verify 定義為連瀏覽器也必須完成，須先补上那份真實證據，再採用該階段完整 accept。

## release：block（等待 v3 複讀與瀏覽器重驗）

目前不可使用先前 `release-review.md` 放行。本人本輪收到的只有 v3 正文與 Node 解析紀錄，尚未讀到 v3 的未看研究／藍圖讀者複讀，以及 v3 瀏覽器重驗結果。這是必要驗收未完成，不是正文品質否定；不能把「有條件 accept」先記成已交付。

解除條件：

1. 讀者對這個 v3 作複讀，揭露曾讀 v1／v2 等條件，確認精度修正未新增理解困難。不需假稱新一輪首次盲測。
2. 瀏覽器實際呈現此 v3，核對三個來源標籤及各自目的地為 S1／S1／S2，記下檢查頁面、版本與觀察；不能只沿用 marked 的輸出。
3. 具名審閱這兩份新證據後另記 release 裁決；正文若再改，重新處理受影響驗收。

補齊且無新問題時，可考慮的唯一成果仍是 **section-draft-ready**。没有真人品味驗收、沒有完整文章修復、沒有發布，更不能以這個試跑推論新版對所有文章均有效。
