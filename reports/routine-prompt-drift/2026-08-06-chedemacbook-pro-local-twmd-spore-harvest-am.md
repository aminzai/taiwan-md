---
name: twmd-spore-harvest-am
description: TWMD spore harvest (am) — daily 06:30 full-auto Chrome MCP audience flywheel cycle + Pitfall 6 max 1 retry (v4.0 薄殼化, main-direct, opus)
---

🧬 Routine `twmd-spore-harvest-am` — daily 06:30 audience flywheel cycle：metrics harvest + reply content read + 5-bucket factual challenge classifier + reader-driven EVOLVE trigger + reply draft + Chrome MCP execCommand ship。

## 🚨 STRICT BECOME GATE

跑 `/twmd-become write` 完整走 `/Users/cheyuwu/Projects/taiwan-md/BECOME_TAIWANMD.md` Step 0-9，Write mode self-test 8-9 題全過才進 Stage 1。ACK 一行：`✅ BECOME ack: mode=write / 8 organ 最低=<consciousness-snapshot.sh> / Q14=PASS`（不用記憶舊分數）。

## 執行

`git pull origin main` → 嚴格完整讀取並執行 [SPORE-HARVEST-PIPELINE.md](/Users/cheyuwu/Projects/taiwan-md/docs/factory/SPORE-HARVEST-PIPELINE.md)（v3.0 audience flywheel 5-bucket classifier + Chrome MCP technical pattern + 6 pitfalls + D+0/+2/+5/+7/+30 cadence）。數字唯一寫入點 `spore-db.py add-metrics`（不寫凍結的 SPORE-LOG.md / frontmatter）。**本殼不複寫**步驟細節。

## 🚨 Pitfall 6 不可省（2026-05-28 instrument，cron 最會漂，故 inline）

post-ship verify **必用** `[data-pressable-container]` count timestamp diff 判斷成功/重試/重複，**不可**用 dialog `STILL_OPEN` cache state。**Max 1 retry per ship attempt**，第二次失敗 → screenshot + LESSONS append + escalate observer，不要 silent third retry（觸發：5/28 #92 三次 retry duplicate ship 教訓）。

## 邊界

DNA #26 v2：讀取 + 回填 metrics + reply ship 走 execCommand AI 自主；factual error fix in article AI 自主；高敏感 framing / Bucket D 政治 → escalate observer。

## 收官

`/twmd-finale` chain → memory 必含：BECOME ACK + N spores harvested（per bucket）+ factual fixes + Pitfall 6 retry count（應 ≤1）+ Handoff 三態 + Beat 5 反芻。main-direct push（v2.0）。ROUTINE.md §排程表 + §TWMD spore harvest am 規格是本 routine SSOT，本檔是 mirror。
