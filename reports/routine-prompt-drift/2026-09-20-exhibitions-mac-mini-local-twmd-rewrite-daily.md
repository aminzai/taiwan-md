---
name: twmd-rewrite-daily
description: TWMD rewrite (daily) — 19:00 full-cycle, read REWRITE-PIPELINE + execute (極簡薄殼 v4.0)
---

🧬 Routine `twmd-rewrite-daily` — daily 18:00 full cycle（article）。

**極簡薄殼**：所有 SOP（Stage 0-5 / hard gates / 搜尋配額 / SSOT 八段 / cron 全 cycle chain / 收官 / 鐵律）100% 在 pipeline canonical。**本 routine 不複寫或補充任何 pipeline 內容**（複寫 = drift = 退化）。三步：

## 1. STRICT BECOME GATE（不可省）

跑 `/twmd-become full` 完整走 `/Users/cheyuwu/Projects/taiwan-md/BECOME_TAIWANMD.md` Step 0-9（full mode，14 題全過）。器官分數跑 `bash /Users/cheyuwu/Projects/taiwan-md/scripts/tools/consciousness-snapshot.sh` 取即時，不用記憶。

## 2. 完整讀 REWRITE-PIPELINE（不可 head / tail / 取樣）

用 **Read tool 一次讀完** `/Users/cheyuwu/Projects/taiwan-md/docs/pipelines/REWRITE-PIPELINE.md`（無 `limit` / `offset`）。pipeline 叫你讀的檔也完整讀。

## 3. 嚴格照 pipeline 執行（cron 全 cycle）

照 pipeline **§Cron 模式 + §Routine 飛輪整合** 跑完整 cycle（PICK → Stage 0-5 article ship → SPORE chain → social broadcast → `/twmd-finale`）。每個 hard gate 都跑（`research-report-health.py` / `article-health.py` …），main-direct push。不跳步、不加碼、不憑記憶。
