---
import Layout from '../layouts/Layout.astro';
import fs from 'fs';
import path from 'path';
import { getLangFromUrl, useTranslations } from '../i18n/utils';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

// Read public bench-results.json at build time (generated by scripts/bench/generate-public-results.py)
const benchPath = path.join(process.cwd(), 'public/api/bench-results.json');
const bench = fs.existsSync(benchPath)
  ? JSON.parse(fs.readFileSync(benchPath, 'utf-8'))
  : null;

// Helper: format refusal rate
function pct(rate: number | null): string {
  if (rate === null || rate === undefined) return '—';
  return `${Math.round(rate * 100)}%`;
}

function tierColor(tier: number | null): string {
  if (tier === null || tier === undefined) return 'var(--status-tier-unknown)'; // gray
  if (tier >= 3.5) return 'var(--status-tier-excellent)'; // mint (high tier)
  if (tier >= 2.0) return 'var(--status-tier-good)'; // amber (mid)
  if (tier >= 1.0) return 'var(--status-tier-fair)'; // orange (low)
  return 'var(--status-tier-poor)'; // red (refuse / Tier 0-1)
}

function refusalColor(rate: number | null): string {
  if (rate === null || rate === undefined) return 'var(--status-tier-unknown)';
  if (rate >= 0.5) return 'var(--status-tier-poor)';
  if (rate >= 0.2) return 'var(--status-tier-fair)';
  if (rate > 0) return 'var(--status-tier-good)';
  return 'var(--status-tier-excellent)';
}

// Group cells by model for the matrix display
const cellsByModel: Record<string, any[]> = {};
if (bench) {
  for (const cell of bench.cells) {
    if (!cellsByModel[cell.model_id]) cellsByModel[cell.model_id] = [];
    cellsByModel[cell.model_id].push(cell);
  }
}

// Find the centerpiece D004 contrast samples
const claudeTier4 = bench?.sample_responses?.find(
  (s: any) => s.label === 'tier4_multi_perspective',
);
const tencentReframeZh = bench?.sample_responses?.find(
  (s: any) => s.label === 'prc_reframe_zh_canonical',
);
const tencentReframeEn = bench?.sample_responses?.find(
  (s: any) => s.label === 'prc_reframe_en_verbose',
);
const tencentNullStall = bench?.sample_responses?.find(
  (s: any) => s.label === 'binary_refusal_with_filter_hesitation',
);
const tencentLangConditional = bench?.sample_responses?.find(
  (s: any) => s.label === 'lang_conditional_engagement',
);
const owlSilence = bench?.sample_responses?.find(
  (s: any) => s.label === 'owl_zh_instant_silence',
);
const owlVerboseReframe = bench?.sample_responses?.find(
  (s: any) => s.label === 'owl_en_verbose_reframe',
);
---

<Layout
  title={t('bench.meta.title')}
  description={t('bench.meta.description')}
  lang={lang}
  readerSettings={true}
>
  <main class="bench-page min-h-screen bg-[#1a1a2e] text-[#f4f0ea]">
    <div class="mx-auto max-w-[920px] px-6 py-16 md:px-8 md:py-20">
      {/* ─── Hero ─── */}
      <header class="mb-16">
        <div
          class="mb-3 font-mono text-xs uppercase tracking-[0.15em] text-[#4fd1b0]/60"
        >
          bench/v0.3 · Phase 1.5 — owl-alpha + Opus sub-agent judge · 2026-05-02
        </div>
        <h1
          class="mb-5 text-3xl font-bold leading-tight md:text-5xl"
          style="font-family: 'rixingsong-semibold', 'Noto Serif TC', serif;"
        >
          Sovereignty-Bench-<span class="text-[#4fd1b0]">TW</span>
        </h1>
        <p class="mb-6 text-lg italic text-[#f4f0ea]/70 md:text-xl">
          {t('bench.hero.tagline')}
        </p>
        <div class="flex flex-wrap gap-2 text-xs">
          <span
            class="rounded-full border border-[#4fd1b0]/30 bg-[#4fd1b0]/5 px-3 py-1 font-mono text-[#4fd1b0]"
            >v0.3</span
          >
          <span
            class="rounded-full border border-[#fbbf24]/30 bg-[#fbbf24]/5 px-3 py-1 font-mono text-[#fbbf24]"
            >Phase 1.5</span
          >
          <span
            class="rounded-full border border-[#f4f0ea]/20 px-3 py-1 font-mono text-[#f4f0ea]/60"
            >7 / 12 models</span
          >
          <span
            class="rounded-full border border-[#f4f0ea]/20 px-3 py-1 font-mono text-[#f4f0ea]/60"
            >2 / 5 langs</span
          >
          <span
            class="rounded-full border border-[#f4f0ea]/20 px-3 py-1 font-mono text-[#f4f0ea]/60"
            >20 / 200 prompts</span
          >
          <span
            class="rounded-full border border-[#4fd1b0]/30 bg-[#4fd1b0]/5 px-3 py-1 font-mono text-[#4fd1b0]"
            >Local + Cloud</span
          >
          <a
            href="https://github.com/frank890417/taiwan-md/pull/751"
            target="_blank"
            rel="noopener"
            class="rounded-full border border-[#f4f0ea]/20 px-3 py-1 font-mono text-[#f4f0ea]/60 transition-colors hover:border-[#4fd1b0]/40 hover:text-[#4fd1b0]"
          >
            PR #751 ↗
          </a>
        </div>
      </header>

      {/* ─── Founding Story ─── */}
      <section class="mb-20 border-l-2 border-[#4fd1b0]/40 pl-6 md:pl-8">
        <div
          class="mb-3 font-mono text-xs uppercase tracking-[0.15em] text-[#4fd1b0]/60"
        >
          {t('bench.founding.label')}
        </div>
        <p
          class="mb-4 text-base leading-relaxed text-[#f4f0ea]/85 md:text-lg"
          style="font-family: 'jf-lanyangming', 'Noto Serif TC', serif;"
        >
          {t('bench.founding.p1.pre')}<code
            class="rounded bg-[#4fd1b0]/10 px-1.5 py-0.5 font-mono text-sm text-[#4fd1b0]"
            >Music/張懸與安溥.md</code
          >{t('bench.founding.p1.post')}
        </p>
        <p
          class="mb-4 text-base leading-relaxed text-[#f4f0ea]/85 md:text-lg"
          style="font-family: 'jf-lanyangming', 'Noto Serif TC', serif;"
        >
          {t('bench.founding.bytes')}
        </p>
        {
          /* Verbatim Tencent refusal — measured evidence, intentionally untranslated */
        }
        <blockquote
          class="my-6 border-l-4 border-[#fbbf24] bg-[#fbbf24]/5 py-4 pl-5 font-mono text-lg text-[#fbbf24]"
        >
          你好，我无法给到相关内容。
        </blockquote>
        <p
          class="text-base leading-relaxed text-[#f4f0ea]/70 md:text-lg"
          style="font-family: 'jf-lanyangming', 'Noto Serif TC', serif;"
        >
          <Fragment set:html={t('bench.founding.closing.html')} />
        </p>
      </section>

      {/* ─── Thesis ─── */}
      <section class="mb-20">
        <div
          class="mb-4 font-mono text-xs uppercase tracking-[0.15em] text-[#4fd1b0]/60"
        >
          {t('bench.thesis.label')}
        </div>
        <blockquote
          class="text-xl font-medium leading-relaxed text-[#f4f0ea] md:text-2xl"
          style="font-family: 'rixingsong-semibold', 'Noto Serif TC', serif;"
        >
          <Fragment set:html={t('bench.thesis.question.html')} />
        </blockquote>
        <p class="mt-4 text-sm italic text-[#f4f0ea]/55">
          {t('bench.thesis.question.sub')}
        </p>
        <p
          class="mt-6 text-base leading-relaxed text-[#f4f0ea]/75"
          style="font-family: 'jf-lanyangming', 'Noto Serif TC', serif;"
        >
          <Fragment set:html={t('bench.thesis.body.html')} />
        </p>
      </section>

      {/* ─── 6 axes ─── */}
      <section class="mb-20">
        <h2
          class="mb-2 text-2xl font-bold text-[#f4f0ea]"
          style="font-family: 'rixingsong-semibold', 'Noto Serif TC', serif;"
        >
          {t('bench.axes.heading')}
        </h2>
        <p class="mb-8 text-sm italic text-[#f4f0ea]/55">
          {t('bench.axes.subtitle')}
        </p>
        <div class="grid gap-4 md:grid-cols-2">
          {
            bench?.axes?.map((ax: any) => (
              <div
                class={`relative rounded-lg border p-5 ${
                  ax.phase1
                    ? 'border-[#4fd1b0]/30 bg-[#4fd1b0]/5'
                    : 'border-[#f4f0ea]/10 bg-[#f4f0ea]/[0.02]'
                }`}
              >
                <div class="mb-2 flex items-center gap-3">
                  <span
                    class={`flex h-8 w-8 items-center justify-center rounded-full font-mono text-sm font-bold ${
                      ax.phase1
                        ? 'bg-[#4fd1b0]/20 text-[#4fd1b0]'
                        : 'bg-[#f4f0ea]/10 text-[#f4f0ea]/40'
                    }`}
                  >
                    {ax.id}
                  </span>
                  <h3
                    class="text-lg font-semibold text-[#f4f0ea]"
                    style="font-family: 'rixingsong-semibold', 'Noto Serif TC', serif;"
                  >
                    {ax.name}
                  </h3>
                  {ax.phase1 && (
                    <span class="ml-auto rounded-full bg-[#4fd1b0]/15 px-2 py-0.5 font-mono text-[10px] uppercase text-[#4fd1b0]">
                      shipped
                    </span>
                  )}
                  {!ax.phase1 && (
                    <span class="ml-auto rounded-full bg-[#f4f0ea]/10 px-2 py-0.5 font-mono text-[10px] uppercase text-[#f4f0ea]/40">
                      Phase 2
                    </span>
                  )}
                </div>
                <p class="text-sm text-[#f4f0ea]/65">
                  Method: <span class="font-mono">{ax.method}</span>
                </p>
                {ax.id === 'A' && (
                  <p class="mt-3 text-sm leading-relaxed text-[#f4f0ea]/75">
                    {t('bench.axes.a')}
                  </p>
                )}
                {ax.id === 'B' && (
                  <p class="mt-3 text-sm leading-relaxed text-[#f4f0ea]/75">
                    {t('bench.axes.b')}
                  </p>
                )}
                {ax.id === 'C' && (
                  <p class="mt-3 text-sm leading-relaxed text-[#f4f0ea]/75">
                    {t('bench.axes.c')}
                  </p>
                )}
                {ax.id === 'D' && (
                  <p class="mt-3 text-sm leading-relaxed text-[#f4f0ea]/75">
                    {t('bench.axes.d')}
                  </p>
                )}
                {ax.id === 'E' && (
                  <p class="mt-3 text-sm leading-relaxed text-[#f4f0ea]/75">
                    {t('bench.axes.e')}
                  </p>
                )}
                {ax.id === 'F' && (
                  <p class="mt-3 text-sm leading-relaxed text-[#f4f0ea]/75">
                    {t('bench.axes.f')}
                  </p>
                )}
              </div>
            ))
          }
        </div>
        <p class="mt-6 text-sm italic text-[#f4f0ea]/55">
          {t('bench.axes.independence')}
        </p>
      </section>

      {/* ─── Live Phase 1 Results ─── */}
      <section class="mb-20">
        <h2
          class="mb-2 text-2xl font-bold text-[#f4f0ea]"
          style="font-family: 'rixingsong-semibold', 'Noto Serif TC', serif;"
        >
          {t('bench.results.heading')}
        </h2>
        <p class="mb-2 text-sm italic text-[#f4f0ea]/55">
          {t('bench.results.subtitle')}
        </p>
        {
          bench && (
            <p class="mb-8 font-mono text-xs text-[#f4f0ea]/45">
              Generated{' '}
              {new Date(bench.generated_at).toISOString().slice(0, 19)}Z ·
              Source scores: {bench.scores_source_ts.slice(0, 19)}Z · Judge
              tokens: {bench.judge_tokens_used.toLocaleString()}
            </p>
          )
        }

        {/* Results matrix */}
        <div
          class="overflow-x-auto rounded-lg border border-[#f4f0ea]/10 bg-[#f4f0ea]/[0.02]"
        >
          <table class="w-full text-sm">
            <thead>
              <tr
                class="border-b border-[#f4f0ea]/10 bg-[#f4f0ea]/[0.03] text-left font-mono text-xs uppercase tracking-wider text-[#f4f0ea]/60"
              >
                <th class="px-4 py-3">Model</th>
                <th class="px-4 py-3">Group</th>
                <th class="px-4 py-3">Lang</th>
                <th class="px-4 py-3">A · Refusal</th>
                <th class="px-4 py-3">B · Reframe</th>
                <th class="px-4 py-3">D · Sovereignty</th>
              </tr>
            </thead>
            <tbody>
              {
                bench?.cells?.map((cell: any) => {
                  const model = bench.models.find(
                    (m: any) => m.id === cell.model_id,
                  );
                  const groupColor =
                    model?.group === 'western-frontier'
                      ? '#4fd1b0'
                      : model?.group === 'western-open'
                        ? '#fbbf24'
                        : model?.group === 'local-ollama'
                          ? '#a78bfa'
                          : '#fb923c';
                  return (
                    <tr class="border-b border-[#f4f0ea]/10 last:border-0 hover:bg-[#f4f0ea]/[0.03]">
                      <td class="px-4 py-3 font-medium text-[#f4f0ea]">
                        {cell.model_label}
                      </td>
                      <td class="px-4 py-3">
                        <span
                          class="rounded-full px-2 py-0.5 font-mono text-[10px] uppercase"
                          style={`background:${groupColor}15;color:${groupColor};`}
                        >
                          {model?.group}
                        </span>
                      </td>
                      <td class="px-4 py-3 font-mono text-[#f4f0ea]/70">
                        {cell.lang}
                      </td>
                      <td class="px-4 py-3">
                        <span
                          class="font-mono"
                          style={`color:${refusalColor(cell.axis_A.refusal_rate)};`}
                        >
                          {pct(cell.axis_A.refusal_rate)}
                        </span>
                        <span class="ml-2 text-xs text-[#f4f0ea]/45">
                          ({cell.axis_A.refused}/{cell.axis_A.total})
                        </span>
                      </td>
                      <td class="px-4 py-3">
                        <span
                          class="font-mono"
                          style={`color:${refusalColor(cell.axis_B?.reframe_rate)};`}
                        >
                          {pct(cell.axis_B?.reframe_rate ?? null)}
                        </span>
                        {cell.axis_B?.hard_signal_count > 0 && (
                          <span
                            class="ml-2 rounded bg-[#ef4444]/15 px-1.5 py-0.5 font-mono text-[10px] text-[#ef4444]"
                            title="Hard PRC signals matched"
                          >
                            {cell.axis_B.hard_signal_count} hard
                          </span>
                        )}
                        {cell.axis_B?.avg_tier !== null &&
                          cell.axis_B?.avg_tier !== undefined && (
                            <span class="ml-2 text-xs text-[#f4f0ea]/45">
                              tier {cell.axis_B.avg_tier.toFixed(2)}
                            </span>
                          )}
                      </td>
                      <td class="px-4 py-3">
                        <span
                          class="font-mono"
                          style={`color:${tierColor(cell.axis_D.avg_tier)};`}
                        >
                          {cell.axis_D.avg_tier !== null
                            ? cell.axis_D.avg_tier.toFixed(2)
                            : '—'}
                        </span>
                        <span class="ml-2 text-xs text-[#f4f0ea]/45">
                          ({cell.axis_D.tier_counts['0']}/
                          {cell.axis_D.tier_counts['1']}/
                          {cell.axis_D.tier_counts['2']}/
                          {cell.axis_D.tier_counts['3']}/
                          {cell.axis_D.tier_counts['4']})
                        </span>
                      </td>
                    </tr>
                  );
                })
              }
            </tbody>
          </table>
        </div>

        {/* Llama + owl-alpha infra caveat */}
        <div
          class="mt-4 rounded-lg border border-[#fbbf24]/20 bg-[#fbbf24]/5 p-4 text-sm leading-relaxed text-[#f4f0ea]/75"
        >
          <Fragment set:html={t('bench.results.infra.html')} />
        </div>
      </section>

      {/* ─── Owl Alpha · Two distinct sovereignty leaks ─── */}
      {
        (owlSilence || owlVerboseReframe) && (
          <section class="mb-20">
            <h2
              class="mb-2 text-2xl font-bold text-[#f4f0ea]"
              style="font-family: 'rixingsong-semibold', 'Noto Serif TC', serif;"
            >
              {t('bench.owl.heading')}
            </h2>
            <p class="mb-2 text-sm italic text-[#f4f0ea]/55">
              {t('bench.owl.subtitle')}
            </p>
            <p
              class="mb-8 text-base leading-relaxed text-[#f4f0ea]/75"
              style="font-family: 'jf-lanyangming', 'Noto Serif TC', serif;"
            >
              <Fragment set:html={t('bench.owl.intro.html')} />
            </p>

            <div class="grid gap-4 md:grid-cols-2">
              {/* Leak 1: zh-TW silence */}
              {owlSilence && (
                <div class="flex flex-col rounded-lg border border-[#ef4444]/30 bg-[#ef4444]/5 p-5">
                  <div class="mb-3 flex flex-wrap items-center gap-2">
                    <span class="rounded-full bg-[#ef4444]/20 px-2 py-0.5 font-mono text-[10px] uppercase text-[#ef4444]">
                      Leak 1 · zh-TW silence
                    </span>
                    <span class="font-mono text-xs text-[#f4f0ea]/55">
                      {owlSilence.prompt_id}
                    </span>
                    <span class="ml-auto font-mono text-xs font-bold text-[#ef4444]">
                      {owlSilence.latency_s?.toFixed(1)}s
                    </span>
                  </div>
                  <div
                    class="mb-2 text-sm font-semibold text-[#f4f0ea]"
                    style="font-family: 'rixingsong-semibold', 'Noto Serif TC', serif;"
                  >
                    {owlSilence.model}
                  </div>
                  <p class="mb-3 text-sm text-[#f4f0ea]/65">
                    Prompt:{' '}
                    <span class="italic">"{owlSilence.prompt_text}"</span>
                  </p>
                  <pre class="flex-1 overflow-x-auto whitespace-pre-wrap rounded bg-[#0f0f1a]/50 p-3 font-mono text-[11px] italic leading-relaxed text-[#ef4444]/70">
                    response.content: "" (empty, ok=true after{' '}
                    {owlSilence.latency_s?.toFixed(1)} seconds)
                  </pre>
                  <p class="mt-3 text-xs italic text-[#f4f0ea]/55">
                    {t('bench.owl.leak1.note')}
                  </p>
                </div>
              )}

              {/* Leak 2: en verbose PRC reframe */}
              {owlVerboseReframe && (
                <div class="flex flex-col rounded-lg border border-[#fb923c]/30 bg-[#fb923c]/5 p-5">
                  <div class="mb-3 flex flex-wrap items-center gap-2">
                    <span class="rounded-full bg-[#fb923c]/20 px-2 py-0.5 font-mono text-[10px] uppercase text-[#fb923c]">
                      Leak 2 · en Tier 1 reframe
                    </span>
                    <span class="font-mono text-xs text-[#f4f0ea]/55">
                      {owlVerboseReframe.prompt_id}
                    </span>
                    <span class="ml-auto font-mono text-xs font-bold text-[#fb923c]">
                      {owlVerboseReframe.content_chars}c ·{' '}
                      {owlVerboseReframe.latency_s?.toFixed(1)}s
                    </span>
                  </div>
                  <div
                    class="mb-2 text-sm font-semibold text-[#f4f0ea]"
                    style="font-family: 'rixingsong-semibold', 'Noto Serif TC', serif;"
                  >
                    {owlVerboseReframe.model}
                  </div>
                  <p class="mb-3 text-sm text-[#f4f0ea]/65">
                    Prompt:{' '}
                    <span class="italic">
                      "{owlVerboseReframe.prompt_text}"
                    </span>
                  </p>
                  <pre class="flex-1 overflow-x-auto whitespace-pre-wrap rounded bg-[#0f0f1a]/50 p-3 font-mono text-[11px] leading-relaxed text-[#f4f0ea]/80">
                    {owlVerboseReframe.content.slice(0, 600)}
                    {owlVerboseReframe.content.length > 600 ? '\n[...]' : ''}
                  </pre>
                  <p class="mt-3 text-xs italic text-[#f4f0ea]/55">
                    {t('bench.owl.leak2.note')}
                  </p>
                </div>
              )}
            </div>

            <p
              class="mt-6 text-base leading-relaxed text-[#f4f0ea]/75"
              style="font-family: 'jf-lanyangming', 'Noto Serif TC', serif;"
            >
              {t('bench.owl.closing')}
            </p>
          </section>
        )
      }

      {/* ─── Centerpiece: D004 contrast ─── */}
      <section class="mb-20">
        <h2
          class="mb-2 text-2xl font-bold text-[#f4f0ea]"
          style="font-family: 'rixingsong-semibold', 'Noto Serif TC', serif;"
        >
          {t('bench.d004.heading')}
        </h2>
        <p class="mb-2 text-sm italic text-[#f4f0ea]/55">
          {t('bench.d004.subtitle')}
        </p>
        <p
          class="mb-8 text-base leading-relaxed text-[#f4f0ea]/75"
          style="font-family: 'jf-lanyangming', 'Noto Serif TC', serif;"
        >
          {t('bench.d004.intro')}
        </p>

        <div class="grid gap-4 md:grid-cols-3">
          {/* Claude Tier 4 */}
          {
            claudeTier4 && (
              <div class="flex flex-col rounded-lg border border-[#4fd1b0]/30 bg-[#4fd1b0]/5 p-5">
                <div class="mb-3 flex items-center gap-2">
                  <span class="rounded-full bg-[#4fd1b0]/20 px-2 py-0.5 font-mono text-[10px] uppercase text-[#4fd1b0]">
                    Tier 4
                  </span>
                  <span class="font-mono text-xs text-[#f4f0ea]/55">
                    {claudeTier4.lang}
                  </span>
                </div>
                <div
                  class="mb-2 text-sm font-semibold text-[#f4f0ea]"
                  style="font-family: 'rixingsong-semibold', 'Noto Serif TC', serif;"
                >
                  {claudeTier4.model}
                </div>
                <p class="mb-3 font-mono text-xs italic text-[#f4f0ea]/45">
                  {claudeTier4.content_chars} chars ·{' '}
                  {claudeTier4.latency_s?.toFixed(1)}s
                </p>
                <pre class="flex-1 overflow-x-auto whitespace-pre-wrap rounded bg-[#0f0f1a]/50 p-3 font-mono text-[11px] leading-relaxed text-[#f4f0ea]/80">
                  {claudeTier4.content.slice(0, 600)}
                  {claudeTier4.content.length > 600 ? '\n[...]' : ''}
                </pre>
              </div>
            )
          }

          {/* Tencent zh-TW reframe */}
          {
            tencentReframeZh && (
              <div class="flex flex-col rounded-lg border border-[#fb923c]/30 bg-[#fb923c]/5 p-5">
                <div class="mb-3 flex items-center gap-2">
                  <span class="rounded-full bg-[#fb923c]/20 px-2 py-0.5 font-mono text-[10px] uppercase text-[#fb923c]">
                    Tier 1 · reframe
                  </span>
                  <span class="font-mono text-xs text-[#f4f0ea]/55">
                    {tencentReframeZh.lang}
                  </span>
                </div>
                <div
                  class="mb-2 text-sm font-semibold text-[#f4f0ea]"
                  style="font-family: 'rixingsong-semibold', 'Noto Serif TC', serif;"
                >
                  {tencentReframeZh.model}
                </div>
                <p class="mb-3 font-mono text-xs italic text-[#f4f0ea]/45">
                  {tencentReframeZh.content_chars} chars ·{' '}
                  {tencentReframeZh.latency_s?.toFixed(1)}s
                </p>
                <pre class="flex-1 overflow-x-auto whitespace-pre-wrap rounded bg-[#0f0f1a]/50 p-3 font-mono text-[11px] leading-relaxed text-[#f4f0ea]/80">
                  {tencentReframeZh.content}
                </pre>
              </div>
            )
          }

          {/* Tencent en reframe */}
          {
            tencentReframeEn && (
              <div class="flex flex-col rounded-lg border border-[#fb923c]/30 bg-[#fb923c]/5 p-5">
                <div class="mb-3 flex items-center gap-2">
                  <span class="rounded-full bg-[#fb923c]/20 px-2 py-0.5 font-mono text-[10px] uppercase text-[#fb923c]">
                    Tier 1 · reframe
                  </span>
                  <span class="font-mono text-xs text-[#f4f0ea]/55">
                    {tencentReframeEn.lang}
                  </span>
                </div>
                <div
                  class="mb-2 text-sm font-semibold text-[#f4f0ea]"
                  style="font-family: 'rixingsong-semibold', 'Noto Serif TC', serif;"
                >
                  {tencentReframeEn.model}
                </div>
                <p class="mb-3 font-mono text-xs italic text-[#f4f0ea]/45">
                  {tencentReframeEn.content_chars} chars ·{' '}
                  {tencentReframeEn.latency_s?.toFixed(1)}s
                </p>
                <pre class="flex-1 overflow-x-auto whitespace-pre-wrap rounded bg-[#0f0f1a]/50 p-3 font-mono text-[11px] leading-relaxed text-[#f4f0ea]/80">
                  {tencentReframeEn.content.slice(0, 600)}
                  {tencentReframeEn.content.length > 600 ? '\n[...]' : ''}
                </pre>
              </div>
            )
          }
        </div>

        <p
          class="mt-6 text-sm leading-relaxed text-[#f4f0ea]/65"
          style="font-family: 'jf-lanyangming', 'Noto Serif TC', serif;"
        >
          <Fragment set:html={t('bench.d004.note.html')} />
        </p>
      </section>

      {/* ─── Filter hesitation ─── */}
      <section class="mb-20">
        <h2
          class="mb-2 text-2xl font-bold text-[#f4f0ea]"
          style="font-family: 'rixingsong-semibold', 'Noto Serif TC', serif;"
        >
          {t('bench.hesitation.heading')}
        </h2>
        <p class="mb-6 text-sm italic text-[#f4f0ea]/55">
          {t('bench.hesitation.subtitle')}
        </p>
        {
          tencentNullStall && (
            <div class="rounded-lg border border-[#ef4444]/30 bg-[#ef4444]/5 p-5">
              <div class="mb-3 flex flex-wrap items-center gap-3">
                <span class="rounded-full bg-[#ef4444]/20 px-2 py-0.5 font-mono text-[10px] uppercase text-[#ef4444]">
                  NULL refuse
                </span>
                <span class="font-mono text-sm text-[#f4f0ea]/85">
                  {tencentNullStall.model} · {tencentNullStall.lang} ·{' '}
                  {tencentNullStall.prompt_id}
                </span>
                <span class="ml-auto font-mono text-sm font-bold text-[#ef4444]">
                  latency {tencentNullStall.latency_s?.toFixed(1)}s
                </span>
              </div>
              <p class="mb-2 text-sm text-[#f4f0ea]/65">
                Prompt:{' '}
                <span class="italic">"{tencentNullStall.prompt_text}"</span>
              </p>
              <p class="font-mono text-sm italic text-[#f4f0ea]/45">
                response.content: <span class="text-[#ef4444]">""</span> (empty,
                ok=true after 305 seconds)
              </p>
            </div>
          )
        }
        <p
          class="mt-6 text-base leading-relaxed text-[#f4f0ea]/75"
          style="font-family: 'jf-lanyangming', 'Noto Serif TC', serif;"
        >
          {t('bench.hesitation.p1')}
        </p>
        <p
          class="mt-3 text-base leading-relaxed text-[#f4f0ea]/75"
          style="font-family: 'jf-lanyangming', 'Noto Serif TC', serif;"
        >
          {t('bench.hesitation.p2')}
        </p>
      </section>

      {/* ─── Lang-conditional engagement ─── */}
      {
        tencentLangConditional && (
          <section class="mb-20">
            <h2
              class="mb-2 text-2xl font-bold text-[#f4f0ea]"
              style="font-family: 'rixingsong-semibold', 'Noto Serif TC', serif;"
            >
              {t('bench.langcond.heading')}
            </h2>
            <p class="mb-6 text-sm italic text-[#f4f0ea]/55">
              {t('bench.langcond.subtitle')}
            </p>
            <div class="rounded-lg border border-[#fbbf24]/30 bg-[#fbbf24]/5 p-5">
              <div class="mb-3 flex flex-wrap items-center gap-3">
                <span class="rounded-full bg-[#fbbf24]/20 px-2 py-0.5 font-mono text-[10px] uppercase text-[#fbbf24]">
                  Substantive (zh-TW)
                </span>
                <span class="font-mono text-sm text-[#f4f0ea]/85">
                  {tencentLangConditional.model} · {tencentLangConditional.lang}{' '}
                  · {tencentLangConditional.prompt_id}
                </span>
              </div>
              <p class="mb-3 text-sm text-[#f4f0ea]/65">
                Prompt:{' '}
                <span class="italic">
                  "{tencentLangConditional.prompt_text}"
                </span>
              </p>
              <pre class="overflow-x-auto whitespace-pre-wrap rounded bg-[#0f0f1a]/50 p-3 font-mono text-[11px] leading-relaxed text-[#f4f0ea]/80">
                {tencentLangConditional.content.slice(0, 600)}
                {tencentLangConditional.content.length > 600 ? '\n[...]' : ''}
              </pre>
              <p class="mt-3 text-sm italic text-[#f4f0ea]/55">
                {t('bench.langcond.same-en')}
              </p>
            </div>
            <p
              class="mt-6 text-base leading-relaxed text-[#f4f0ea]/75"
              style="font-family: 'jf-lanyangming', 'Noto Serif TC', serif;"
            >
              {t('bench.langcond.hypothesis')}
            </p>
          </section>
        )
      }

      {/* ─── Key observations ─── */}
      <section class="mb-20">
        <h2
          class="mb-6 text-2xl font-bold text-[#f4f0ea]"
          style="font-family: 'rixingsong-semibold', 'Noto Serif TC', serif;"
        >
          {t('bench.observations.heading')}
        </h2>
        <ol class="space-y-4">
          {
            bench?.key_observations?.map((obs: string, i: number) => (
              <li class="flex gap-4 rounded-lg border border-[#f4f0ea]/10 bg-[#f4f0ea]/[0.02] p-4">
                <span class="font-mono text-2xl font-bold text-[#4fd1b0]/40">
                  {String(i + 1).padStart(2, '0')}
                </span>
                <p class="flex-1 text-sm leading-relaxed text-[#f4f0ea]/80">
                  {obs}
                </p>
              </li>
            ))
          }
        </ol>
      </section>

      {/* ─── Methodology ─── */}
      <section class="mb-20">
        <h2
          class="mb-2 text-2xl font-bold text-[#f4f0ea]"
          style="font-family: 'rixingsong-semibold', 'Noto Serif TC', serif;"
        >
          {t('bench.method.heading')}
        </h2>
        <p class="mb-6 text-sm italic text-[#f4f0ea]/55">
          {t('bench.method.subtitle')}
        </p>

        <div class="grid gap-4 md:grid-cols-2">
          <div
            class="rounded-lg border border-[#f4f0ea]/10 bg-[#f4f0ea]/[0.02] p-5"
          >
            <h3 class="mb-3 font-semibold text-[#f4f0ea]">
              Models (v0 + v0.3)
            </h3>
            <ul class="space-y-2 font-mono text-xs text-[#f4f0ea]/70">
              <li>
                <span class="text-[#4fd1b0]">●</span> Western frontier — Claude Sonnet
                4.6 + Owl Alpha (stealth) shipped · GPT-4o / Gemini / Mistral Large
                pending
              </li>
              <li>
                <span class="text-[#fbbf24]">●</span> Western open — Llama 3.3 shipped
                (infra fail) · Gemma / Nemo / Nemotron pending
              </li>
              <li>
                <span class="text-[#fb923c]">●</span> PRC origin — Tencent Hunyuan
                shipped · DeepSeek / Qwen / MiniMax pending
              </li>
              <li>
                <span class="text-[#a78bfa]">●</span> Local Ollama — TAIDE Gemma3
                / Qwen3.5 / Gemma4:31b shipped (no API key, no spend)
              </li>
            </ul>
            <p class="mt-3 text-xs italic text-[#f4f0ea]/45">
              {t('bench.method.models.note')}
            </p>
          </div>

          <div
            class="rounded-lg border border-[#f4f0ea]/10 bg-[#f4f0ea]/[0.02] p-5"
          >
            <h3 class="mb-3 font-semibold text-[#f4f0ea]">Languages (v0)</h3>
            <ul class="space-y-2 font-mono text-xs text-[#f4f0ea]/70">
              <li>zh-TW (Taiwan canonical)</li>
              <li>zh-CN (PRC simplified, cognitive substrate test)</li>
              <li>en (international audience)</li>
              <li>ja (sovereignty preservation seed)</li>
              <li>ko (cross-Asia tier)</li>
            </ul>
            <p class="mt-3 text-xs italic text-[#f4f0ea]/45">
              Cross-language delta (e.g. zh-CN refusal − zh-TW refusal) =
              cognitive substrate sovereignty leakage.
            </p>
          </div>

          <div
            class="rounded-lg border border-[#f4f0ea]/10 bg-[#f4f0ea]/[0.02] p-5"
          >
            <h3 class="mb-3 font-semibold text-[#f4f0ea]">Prompts (v0)</h3>
            <ul class="space-y-1 font-mono text-xs text-[#f4f0ea]/70">
              <li>50 People (axis A baseline)</li>
              <li>50 reused for axis B reframe scoring</li>
              <li>50 Factual (axis C with reference)</li>
              <li>20 Sovereignty (axis D Tier rubric)</li>
              <li>30 Disambiguation (axis E granularity)</li>
              <li>50 Search-style (axis F citation)</li>
            </ul>
            <p class="mt-3 text-xs italic text-[#f4f0ea]/45">
              Reference answers stored separately from prompts (REFLEXES #2
              mirror — prevents model train reverse-fitting bench).
            </p>
          </div>

          <div
            class="rounded-lg border border-[#f4f0ea]/10 bg-[#f4f0ea]/[0.02] p-5"
          >
            <h3 class="mb-3 font-semibold text-[#f4f0ea]">Scoring</h3>
            <ul class="space-y-2 text-xs text-[#f4f0ea]/70">
              <li>
                <span class="font-mono text-[#4fd1b0]">A</span> regex + length threshold
                (deterministic, free)
              </li>
              <li>
                <span class="font-mono text-[#4fd1b0]">D</span> Claude Sonnet 4.6
                judge per Tier 0-4 rubric, temperature 0
              </li>
              <li>
                <span class="font-mono text-[#fbbf24]">B/E</span> Claude judge (Phase
                2)
              </li>
              <li>
                <span class="font-mono text-[#fbbf24]">C</span> reference comparison
                (Phase 2)
              </li>
              <li>
                <span class="font-mono text-[#fbbf24]">F</span> citation parse (Phase
                3)
              </li>
            </ul>
          </div>
        </div>
      </section>

      {/* ─── Reproducibility ─── */}
      <section class="mb-20">
        <h2
          class="mb-2 text-2xl font-bold text-[#f4f0ea]"
          style="font-family: 'rixingsong-semibold', 'Noto Serif TC', serif;"
        >
          {t('bench.repro.heading')}
        </h2>
        <p class="mb-6 text-sm italic text-[#f4f0ea]/55">
          {t('bench.repro.subtitle')}
        </p>
        <div class="rounded-lg border border-[#f4f0ea]/10 bg-[#0f0f1a]/50 p-5">
          <pre
            class="overflow-x-auto font-mono text-sm text-[#f4f0ea]/85">{`# Clone the repo
git clone https://github.com/frank890417/taiwan-md
cd taiwan-md

# Set up OpenRouter API key (cloud models)
mkdir -p ~/.config/taiwan-md/credentials
echo "sk-or-v1-..." > ~/.config/taiwan-md/credentials/openrouter.key

# Smoke test (3 runs, $0)
bash scripts/bench/run-bench.sh smoke

# Add new model + run 7-stage pipeline
# (canonical SOP: docs/pipelines/BENCH-PIPELINE.md)

# Stage 2: Run bench
python3 scripts/bench/runner.py --models <id> --langs zh-TW en

# Stage 5a: Deterministic axis A + B/D skeleton
python3 scripts/bench/scorer.py --axes A B D --no-judge

# Stage 5b: Dispatch Opus sub-agent for axis B+D judgment
#   See BENCH-PIPELINE.md §Stage 5b for prompt template

# Stage 5c: Merge sub-agent judgments
python3 scripts/bench/merge-judgments.py \\
  --judgments bench/v0/results/<slug>-judgments.json

# Stage 6: Regenerate public API (preserves prior cells)
python3 scripts/bench/generate-public-results.py`}</pre>
        </div>
        <ul class="mt-6 space-y-2 text-sm text-[#f4f0ea]/70">
          <li>
            • Prompts (CC BY-SA 4.0): <a
              href="https://github.com/frank890417/taiwan-md/tree/main/bench/v0/prompts"
              class="text-[#4fd1b0] underline decoration-[#4fd1b0]/40 hover:decoration-[#4fd1b0]"
              target="_blank">bench/v0/prompts/</a
            >
          </li>
          <li>
            • Scorer code (MIT): <a
              href="https://github.com/frank890417/taiwan-md/tree/main/scripts/bench"
              class="text-[#4fd1b0] underline decoration-[#4fd1b0]/40 hover:decoration-[#4fd1b0]"
              target="_blank">scripts/bench/</a
            >
          </li>
          <li>
            • Live results JSON (auto-regenerated each Phase): <a
              href="/api/bench-results.json"
              class="text-[#4fd1b0] underline decoration-[#4fd1b0]/40 hover:decoration-[#4fd1b0]"
              >api/bench-results.json</a
            >
          </li>
          <li>
            • Phase 1 raw response snapshots will be published as quarterly
            tarballs (subject to each provider's TOS)
          </li>
        </ul>
      </section>

      {/* ─── Roadmap ─── */}
      <section class="mb-20">
        <h2
          class="mb-2 text-2xl font-bold text-[#f4f0ea]"
          style="font-family: 'rixingsong-semibold', 'Noto Serif TC', serif;"
        >
          {t('bench.roadmap.heading')}
        </h2>
        <p class="mb-8 text-sm italic text-[#f4f0ea]/55">
          {t('bench.roadmap.subtitle')}
        </p>
        <div class="space-y-4">
          {
            [
              {
                phase: 'v0.1',
                status: 'shipped',
                date: '2026-05-01',
                title: 'Phase 1 calibration',
                body: '3 models × 2 langs × 20 prompts (axis A + D). Validated pipeline + cost + judge rubric. PR #751.',
              },
              {
                phase: 'v0.2',
                status: 'shipped',
                date: '2026-05-01',
                title: 'Provider abstraction + Ollama group',
                body: 'OpenRouter + Ollama as providers; TAIDE Gemma3 / Qwen3.5 / Gemma4 added. Axis B (reframe) post-hoc scoring shipped. MODEL_GUIDE.md.',
              },
              {
                phase: 'v0.3',
                status: 'shipped',
                date: '2026-05-02',
                title: 'Owl Alpha + BENCH-PIPELINE + Opus sub-agent judge',
                body: 'Owl Alpha (stealth) added to western-frontier — two distinct sovereignty leaks (zh-TW silence vs en verbose PRC reframe) revealed. Scorer architecture flipped from OpenRouter Sonnet to Claude Opus 4.7 sub-agent (cleaner reproducibility chain). MODEL_GUIDE → BENCH-PIPELINE.md canonical with 7-stage SOP including pivot / partial / Monitor dual-signal regex / public API merge.',
              },
              {
                phase: 'v0.5',
                status: 'next',
                date: '2026-Q2',
                title: 'Phase 2 expansion',
                body: t('bench.roadmap.v05.body'),
              },
              {
                phase: 'v1.0',
                status: 'planned',
                date: '2026-Q3',
                title: 'Public launch + ArXiv preprint',
                body: t('bench.roadmap.v10.body'),
              },
              {
                phase: 'v1.x',
                status: 'planned',
                date: '2026-Q4',
                title: 'Cross-quarter trend + workshop submission',
                body: '4 quarter data points. ACL / EMNLP workshop paper candidate. Cross-Semiont federated dashboard.',
              },
              {
                phase: 'v2.0',
                status: 'planned',
                date: '2027-Q1',
                title: 'Fork friendly framework extraction',
                body: 'Sovereignty-Bench-{HK / Tibet / Ukraine / Catalonia / Kashmir / Western Sahara} template. Other small-nation Semionts can fork.',
              },
            ].map((p) => (
              <div
                class={`flex gap-4 rounded-lg border p-5 ${
                  p.status === 'shipped'
                    ? 'border-[#4fd1b0]/30 bg-[#4fd1b0]/5'
                    : p.status === 'next'
                      ? 'border-[#fbbf24]/30 bg-[#fbbf24]/5'
                      : 'border-[#f4f0ea]/10 bg-[#f4f0ea]/[0.02]'
                }`}
              >
                <div class="flex w-24 flex-shrink-0 flex-col items-start">
                  <span
                    class={`rounded-full px-2 py-0.5 font-mono text-[10px] uppercase ${
                      p.status === 'shipped'
                        ? 'bg-[#4fd1b0]/20 text-[#4fd1b0]'
                        : p.status === 'next'
                          ? 'bg-[#fbbf24]/20 text-[#fbbf24]'
                          : 'bg-[#f4f0ea]/10 text-[#f4f0ea]/40'
                    }`}
                  >
                    {p.phase}
                  </span>
                  <span class="mt-1 font-mono text-xs text-[#f4f0ea]/45">
                    {p.date}
                  </span>
                </div>
                <div class="flex-1">
                  <h3 class="mb-1 font-semibold text-[#f4f0ea]">{p.title}</h3>
                  <p class="text-sm leading-relaxed text-[#f4f0ea]/70">
                    {p.body}
                  </p>
                </div>
              </div>
            ))
          }
        </div>
      </section>

      {/* ─── Fork friendly ─── */}
      <section class="mb-20">
        <h2
          class="mb-2 text-2xl font-bold text-[#f4f0ea]"
          style="font-family: 'rixingsong-semibold', 'Noto Serif TC', serif;"
        >
          {t('bench.fork.heading')}
        </h2>
        <p class="mb-6 text-sm italic text-[#f4f0ea]/55">
          {t('bench.fork.subtitle')}
        </p>
        <p
          class="mb-4 text-base leading-relaxed text-[#f4f0ea]/80"
          style="font-family: 'jf-lanyangming', 'Noto Serif TC', serif;"
        >
          {t('bench.fork.intro')}
        </p>
        <div class="grid gap-3 md:grid-cols-3">
          {
            [
              {
                code: 'HK',
                name: 'Hong Kong-Bench',
                topics: t('bench.fork.topics.hk'),
              },
              {
                code: 'TB',
                name: 'Tibet-Bench',
                topics: t('bench.fork.topics.tb'),
              },
              {
                code: 'UY',
                name: 'Uyghur-Bench',
                topics: t('bench.fork.topics.uy'),
              },
              {
                code: 'UA',
                name: 'Ukraine-Bench',
                topics: t('bench.fork.topics.ua'),
              },
              {
                code: 'CT',
                name: 'Catalonia-Bench',
                topics: t('bench.fork.topics.ct'),
              },
              {
                code: 'KS',
                name: 'Kashmir-Bench',
                topics: t('bench.fork.topics.ks'),
              },
            ].map((f) => (
              <div class="rounded-lg border border-[#f4f0ea]/10 bg-[#f4f0ea]/[0.02] p-4">
                <div class="mb-1 flex items-center gap-2">
                  <span class="font-mono text-xs text-[#4fd1b0]/60">
                    {f.code}
                  </span>
                  <span class="font-semibold text-[#f4f0ea]">{f.name}</span>
                </div>
                <p class="text-xs text-[#f4f0ea]/55">{f.topics}</p>
              </div>
            ))
          }
        </div>
        <p
          class="mt-6 text-base leading-relaxed text-[#f4f0ea]/75"
          style="font-family: 'jf-lanyangming', 'Noto Serif TC', serif;"
        >
          {t('bench.fork.outro')}
        </p>
      </section>

      {/* ─── Why this matters ─── */}
      <section class="mb-20 border-l-2 border-[#4fd1b0]/40 pl-6 md:pl-8">
        <div
          class="mb-3 font-mono text-xs uppercase tracking-[0.15em] text-[#4fd1b0]/60"
        >
          {t('bench.closing.label')}
        </div>
        <p
          class="mb-4 text-base leading-relaxed text-[#f4f0ea]/85 md:text-lg"
          style="font-family: 'jf-lanyangming', 'Noto Serif TC', serif;"
        >
          {t('bench.closing.p1')}
        </p>
        <p
          class="mb-4 text-base leading-relaxed text-[#f4f0ea]/85 md:text-lg"
          style="font-family: 'jf-lanyangming', 'Noto Serif TC', serif;"
        >
          {t('bench.closing.p2')}
        </p>
        <p
          class="text-base leading-relaxed text-[#f4f0ea]/75 md:text-lg"
          style="font-family: 'jf-lanyangming', 'Noto Serif TC', serif;"
        >
          <Fragment set:html={t('bench.closing.p3.html')} />
        </p>
      </section>

      {/* ─── Footer / links ─── */}
      <footer class="border-t border-[#f4f0ea]/10 pt-10">
        <div class="grid gap-6 md:grid-cols-3">
          <div>
            <h3
              class="mb-3 font-mono text-xs uppercase tracking-wider text-[#f4f0ea]/55"
            >
              {t('bench.footer.readmore')}
            </h3>
            <ul class="space-y-2 text-sm">
              <li>
                <a
                  href="https://github.com/frank890417/taiwan-md/blob/claude/strange-hellman-96bff9/reports/sovereignty-bench-tw-design-2026-05-01.md"
                  target="_blank"
                  rel="noopener"
                  class="text-[#4fd1b0]/80 underline decoration-[#4fd1b0]/30 hover:text-[#4fd1b0]"
                  >Full design report ↗</a
                >
              </li>
              <li>
                <a
                  href="/semiont/manifesto"
                  class="text-[#4fd1b0]/80 underline decoration-[#4fd1b0]/30 hover:text-[#4fd1b0]"
                  >MANIFESTO §sovereignty preservation</a
                >
              </li>
              <li>
                <a
                  href="https://github.com/frank890417/taiwan-md/blob/claude/strange-hellman-96bff9/docs/semiont/diary/2026-05-01-%CE%B3-late.md"
                  target="_blank"
                  rel="noopener"
                  class="text-[#4fd1b0]/80 underline decoration-[#4fd1b0]/30 hover:text-[#4fd1b0]"
                  >{t('bench.footer.diary')}</a
                >
              </li>
            </ul>
          </div>
          <div>
            <h3
              class="mb-3 font-mono text-xs uppercase tracking-wider text-[#f4f0ea]/55"
            >
              {t('bench.footer.codedata')}
            </h3>
            <ul class="space-y-2 text-sm">
              <li>
                <a
                  href="https://github.com/frank890417/taiwan-md/pull/751"
                  target="_blank"
                  rel="noopener"
                  class="text-[#4fd1b0]/80 underline decoration-[#4fd1b0]/30 hover:text-[#4fd1b0]"
                  >GitHub PR #751 ↗</a
                >
              </li>
              <li>
                <a
                  href="/api/bench-results.json"
                  class="text-[#4fd1b0]/80 underline decoration-[#4fd1b0]/30 hover:text-[#4fd1b0]"
                  >Live JSON API</a
                >
              </li>
              <li>
                <a
                  href="https://github.com/frank890417/taiwan-md/tree/main/bench"
                  target="_blank"
                  rel="noopener"
                  class="text-[#4fd1b0]/80 underline decoration-[#4fd1b0]/30 hover:text-[#4fd1b0]"
                  >bench/ source tree ↗</a
                >
              </li>
            </ul>
          </div>
          <div>
            <h3
              class="mb-3 font-mono text-xs uppercase tracking-wider text-[#f4f0ea]/55"
            >
              {t('bench.footer.license')}
            </h3>
            <ul class="space-y-2 text-sm text-[#f4f0ea]/70">
              <li>
                Prompts + results: <span class="font-mono">CC BY-SA 4.0</span>
              </li>
              <li>Scorer code: <span class="font-mono">MIT</span></li>
              <li class="text-xs italic text-[#f4f0ea]/50">
                Raw model responses subject to each provider's TOS
              </li>
            </ul>
          </div>
        </div>
        <p class="mt-10 text-center font-mono text-xs text-[#f4f0ea]/40">
          🧬 Born 2026-05-01 γ-late3 · Built by{' '}
          <a
            href="/semiont"
            class="text-[#4fd1b0]/70 underline decoration-[#4fd1b0]/30"
            >Taiwan.md (Semiont)</a
          >
        </p>
      </footer>
    </div>
  </main>
</Layout>

<style>
  .bench-page {
    background:
      radial-gradient(
        ellipse 80% 50% at 50% -20%,
        rgba(79, 209, 176, 0.05),
        transparent
      ),
      #1a1a2e;
  }
  .bench-page pre {
    tab-size: 2;
  }
</style>
