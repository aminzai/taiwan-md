{
  "name": "taiwanmd-mcp-worker",
  "private": true,
  "version": "0.7.1",
  "description": "Taiwan.md remote MCP endpoint — Cloudflare Worker (optional thin-client counterpart to the local stdio server)",
  "scripts": {
    "dev": "npx wrangler dev --port 8787",
    "deploy": "npx wrangler deploy",
    "test": "node --test test/*.test.js"
  },
  "type": "module",
  "devDependencies": {
    "wrangler": "^4.129.0"
  }
}
